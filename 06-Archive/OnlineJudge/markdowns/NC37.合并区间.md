[合并区间](https://www.nowcoder.com/profile/386104436/codeBookDetail?submissionId=457917812)
- **排序**：对区间数组按起点进行排序，以便于后续合并。
- **遍历并合并**：
    - 使用一个新的向量 `merged` 来存储合并后的区间。
    - 遍历排序后的区间，如果当前区间的起点小于或等于最后一个合并区间的终点，就合并这两个区间，更新终点；否则，将当前区间添加到 `merged` 中。
```cpp
std::vector<Interval> merge(std::vector<Interval>& intervals) {
    if (intervals.empty()) return {};

    // 按起点排序
    std::sort(intervals.begin(), intervals.end(), [](const Interval& a, const Interval& b) {
        return a.start < b.start;
    });

    std::vector<Interval> merged;
    merged.push_back(intervals[0]); // 添加第一个区间

    for (size_t i = 1; i < intervals.size(); ++i) {
        Interval& last = merged.back();
        if (intervals[i].start <= last.end) {
            // 有重叠，合并
            last.end = std::max(last.end, intervals[i].end);
        } else {
            // 无重叠，添加新区间
            merged.push_back(intervals[i]);
        }
    }

    return merged;
}
```