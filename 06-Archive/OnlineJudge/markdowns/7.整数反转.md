[7. 整数反转](https://leetcode.cn/problems/reverse-integer/) 

##### 方法一
- 每次取末尾一位构造反转后的数字
- 若已构造部分达到`214748364`, 剩余一位就得判断是否大于7，或当前部分大于`214748364`
- 负数同理
```cpp
    int reverse(int x) {
        int res = 0;
        while(x!=0) {
            //每次取末尾数字
            int tmp = x%10;
            //判断是否 大于 最大32位整数
            if (res>214748364 || (res==214748364 && tmp>7)) {
                return 0;
            }
            //判断是否 小于 最小32位整数
            if (res<-214748364 || (res==-214748364 && tmp<-8)) {
                return 0;
            }
            res = res*10 + tmp;
            x /= 10;
        }
        return res;
    }		
```


---
##### 方法二数学
- 记 rev 为翻转后的数字，为完成翻转，我们可以重复「弹出」x 的末尾数字，将其「推入」rev 的末尾，直至 x 为 0。
- 要在没有辅助栈或数组的帮助下「弹出」和「推入」数字，可以使用如下数学方法：

```cpp
// 弹出 x 的末尾数字 digit
digit = x % 10
x /= 10

// 将数字 digit 推入 rev 末尾
rev = rev * 10 + digit
```

- 题目需要判断反转后的数字是否超过 32 位有符号整数的范围 \[$−2^{31},2^{31}−1$\]，例如 x=2123456789 反转后的 rev=9876543212>$2^{31}−1$= 2147483647，超过了 32 位有符号整数的范围

- 因此我们需要在「推入」数字之前，判断是否满足$−2^{31}≤rev⋅10+digit≤2^{31}−1$ 若该不等式不成立则返回 0

- 但是题目要求不允许使用 64 位整数，即运算过程中的数字必须在 32 位有符号整数的范围内，因此我们不能直接按照上述式子计算，需要另寻他路

考虑 x>0 的情况，记 $INT\_MAX=2^{31}−1=2147483647$，由于$$INT\_MAX​=⌊\frac{INT\_MAX}{10}​⌋⋅10+(INT\_MAX \mod 10)=⌊\frac{INT\_MAX}{10}​⌋+7​$$

则不等式$rev⋅10+digit≤INT\_MAX$

等价于$rev⋅10+digit≤⌊\frac{INT\_MAX}{10}​⌋⋅10+7$

移项得$(rev−⌊\frac{INT\_MAX}{10}​⌋)⋅10≤7−digit$

讨论该不等式成立的条件：
- 若 $rev>⌊\frac{INT\_MAX}{10}​⌋$，由于 digit≥0，不等式不成立。  
- 若 $rev=⌊\frac{INT\_MAX}{10}​⌋$，当且仅当 digit≤7 时，不等式成立。  
- 若 $rev<⌊\frac{INT\_MAX}{10}​⌋$，由于 digit≤9，不等式成立。

注意到当 $rev=⌊\frac{INT\_MAX}{10}​⌋$ 时若还能推入数字，则说明 x 的位数与 $INT\_MAX$ 的位数相同，且要推入的数字 digit 为 x 的最高位。由于 x 不超过 $INT\_MAX$，因此 digit 不会超过 $INT\_MAX$ 的最高位，即 $digit≤2$。所以实际上当 $rev=⌊\frac{INT\_MAX}{10}​⌋$ 时不等式必定成立

因此判定条件可简化为：**当且仅当 $rev≤⌊10INT\_MAX​⌋$ 时，不等式成立**

x<0 的情况类似，留给读者自证，此处不再赘述

综上所述，判断不等式

$−2^{31}≤rev⋅10+digit≤2^{31}−1$

是否成立，可改为判断不等式

$⌈\frac{-2^{31}}{10}​⌉≤rev≤⌊\frac{2^{31}−1}{10}​⌋$

是否成立，若不成立则返回 0
```cpp
    int reverse(int x) {
        int rev = 0;
        while (x != 0) {
            if (rev < INT_MIN / 10 || rev > INT_MAX / 10) {
                return 0;
            }
            int digit = x % 10;
            x /= 10;
            rev = rev * 10 + digit;
        }
        return rev;
    }
```

---
##### 模拟
- 对于待反转的数字`x`, 若反转之后溢出，那么位数一定为10位
- 此时对于从小到大每一位，若该位大于最大值该位数值，那么结果为0
	- 若小于该位最大数值，那么就不会发生溢出，即可以直接反转
	- 若等于，进行下一位判断，直到最后一位

- 反转时将`int`转为字符串，使用`std::reverse()`后再转回即可

```cpp
    int reverse(int x) {
       if (x == -2147483648) return 0;

        if (x > 0) {
            int tx = x;
            while (x > 1000000000) {
                int b = 0;
                
                b = x % 10;
                if (b > 2) return 0;
                else if (b < 2) break;
                x /= 10;
                b = x % 10;
                if (b > 1) return 0;
                else if (b < 1) break;
                x /= 10;
                b = x % 10;
                if (b > 4) return 0;
                else if (b < 4) break;
                x /= 10;
                b = x % 10;
                if (b > 7) return 0;
                else if (b < 7) break;
                x /= 10;
                b = x % 10;
                if (b > 4) return 0;
                else if (b < 4) break;
                x /= 10;
                b = x % 10;
                if (b > 8) return 0;
                else if (b < 8) break;
                x /= 10;
                b = x % 10;
                if (b > 3) return 0;
                else if (b < 3) break;
                x /= 10;
                b = x % 10;
                if (b > 6) return 0;
                else if (b < 6) break;
                x /= 10;
                b = x % 10;
                if (b > 4) return 0;
                else if (b < 4) break;
                x /= 10;
                b = x % 10;
                if (b > 7) return 0;
                else if (b < 7) break;
            }

            std::string str = std::to_string(tx);
            std::reverse(str.begin(), str.end());
            return stoi(str);
        } else {
            while (x < -1000000000) {
                int b = 0;
                int tx = -x;
                b = tx % 10;
                if (b > 2)
                    return 0;
                else if (b < 2)
                    break;
                tx /= 10;
                b = tx % 10;
                if (b > 1)
                    return 0;
                else if (b < 1)
                    break;
                tx /= 10;
                b = tx % 10;
                if (b > 4)
                    return 0;
                else if (b < 4)
                    break;
                tx /= 10;
                b = tx % 10;
                if (b > 7)
                    return 0;
                else if (b < 7)
                    break;
                tx /= 10;
                b = tx % 10;
                if (b > 4)
                    return 0;
                else if (b < 4)
                    break;
                tx /= 10;
                b = tx % 10;
                if (b > 8)
                    return 0;
                else if (b < 8)
                    break;
                tx /= 10;
                b = tx % 10;
                if (b > 3)
                    return 0;
                else if (b < 3)
                    break;
                tx /= 10;
                b = tx % 10;
                if (b > 6)
                    return 0;
                else if (b < 6)
                    break;
                tx /= 10;
                b = tx % 10;
                if (b > 4)
                    return 0;
                else if (b < 4)
                    break;
                tx /= 10;
                b = tx % 10;
                if (b > 7)
                    return 0;
                else if (b < 7)
                    break;
            }

            std::string str = std::to_string(-x);
            std::reverse(str.begin(), str.end());
            return -stoi(str);
        }