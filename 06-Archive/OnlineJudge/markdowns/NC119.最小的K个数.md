[最小的K个数](https://www.nowcoder.com/profile/386104436/codeBookDetail?submissionId=457591521) 
- 使用`std::sort`
- 使用优先队列创建堆
```cpp
    vector<int> GetLeastNumbers_Solution(vector<int>& input, int k) {
        // write code here
        // std::sort(input.begin(),input.end());
        // return vector<int>(input.begin(),input.begin()+k);
        // 使用哈希表统计元素频率
        // unordered_map<int, int> freq;
        // for (int num : input) {
        //     freq[num]++;
        // }

        // 使用最小堆来存储最小的 k 个数
        priority_queue<int, vector<int>, greater<int>> minHeap;

        for (int i:input ) {
            minHeap.push(i);
            // 保持堆的大小为 k
        }

        // 收集结果
        vector<int> result;
        for (int i = 0; i < k; ++i) {
            result.push_back(minHeap.top());
            minHeap.pop();
        }

        return result;
    }
```