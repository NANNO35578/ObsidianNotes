[二叉树最大深度](https://www.nowcoder.com/profile/386104436/codeBookDetail?submissionId=456680652)
- 可使用BFS 层序遍历 或者递归调用
```cpp
    int maxDepth(TreeNode* root) {
        // write code here
        // * 层序遍历
        // queue<TreeNode*> level;
        // level.push(root);
        // level.push(nullptr);
        // int ans = 1;
        // while (level.size() != 1) {
        //     TreeNode* pL = level.front();
        //     level.pop();
        //     if (pL == nullptr) {
        //         ++ans;
        //         level.push(nullptr);
        //     } else {
        //         if (pL->left != nullptr) level.push(pL->left);
        //         if (pL->right != nullptr) level.push(pL->right);
        //     }
        // }
        // return ans;

        //* 递归
        if (root == nullptr) {
            return 0;  // 空树深度为0
        }
        int leftDepth = maxDepth(root->left);    // 计算左子树深度
        int rightDepth = maxDepth(root->right);  // 计算右子树深度
        return max(leftDepth, rightDepth) + 1;   // 返回最大深度加1
    }
```