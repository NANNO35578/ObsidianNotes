你是一名资深的vue3页面开发工程师. 
当前系统是一个基于vue3的笔记前端项目. 你需要完成推荐页面开发.

## 需求
针对新开发的API实现页面

## API

| 接口                     | 方法  | 说明   | api.md文档位置                |
| ---------------------- | --- | ---- | ------------------------- |
| `/api/recommend/notes` | GET | 推荐笔记 | `./docs/api.md L:629~667` |

- 严格按照API文档实现接口

## 页面设计
- 在搜索按钮(`./calcite-web/src/components/center/CenterToolbar.vue`中的toolbar-center部分)旁边新增一个获取推荐按钮
- 用户点击获取推荐按钮后, Home.vue中`main-content`部分显示*预览视图*
- 预览视图同样使用列表.
- 点击列表中的笔记卡片后, 进入预览页面(`./calcite-web/src/components/center/PublicNotePreview.vue`)

## 输出
1. 你自己列好TODO, 按照TODO完成任务
2. 将你的操作在./docs中形成以260421_为前缀的文档



````markdown title:"无惊无喜"
推荐页面开发已完成，所有任务均通过验证。以下是执行摘要：

---

## 修改文件

| 文件 | 修改内容 |
|------|---------|
| `calcite-web/src/api/note.js` | 新增 `getRecommendNotes` 接口 |
| `calcite-web/src/components/center/CenterToolbar.vue` | 搜索按钮旁新增"获取推荐"按钮（MagicStick 图标） |
| `calcite-web/src/views/Home.vue` | 集成推荐模式状态、数据获取、视图切换逻辑 |
| `docs/260421_recommend_page.md` | 操作文档 |

## 核心实现逻辑

1. **API 层**：`GET /recommend/notes`，支持 `page` / `page_size` 分页参数
2. **工具栏**：在 `toolbar-center` 区域新增按钮，点击 emit `recommend` 事件
3. **Home.vue 状态**：新增 `recommending` / `recommendNotes` / `recommendLoading` / `recommendPage` / `recommendPageSize`
4. **视图复用**：推荐列表直接复用现有 `NoteListView` → `NoteList` → `NoteCard` 组件栈
   - `displayNotes` computed：推荐模式下返回 `recommendNotes`
   - `contentTitle` computed：推荐模式下显示"推荐笔记"
5. **点击行为**：`handleListNoteClick` 包装器判断当前模式，推荐模式下直接调用 `handlePublicNoteClick` 进入预览
6. **模式互斥**：搜索 / 点击文件夹 / 创建笔记时自动退出推荐模式

## 验证

`npm run build` 编译通过，无报错。
````