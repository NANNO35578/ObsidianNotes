当前项目是drogon框架的C++后段项目.
你现在需要补充./calcite/test中的CMakeLists.txt, 以便能够生成测试可执行文件.

理论上你不需要阅读代码内容, 除了./calcite/CMakeLists.txt这是编译正式版的. 你只需要分析./calcite这个项目结构就可以生成./calcite/test/CMakeLists.txt.



````markdown title:""
已完成 `./calcite/test/CMakeLists.txt` 的编写，并验证构建成功。

生成的文件核心逻辑：
- 使用 `aux_source_directory` 收集 `../controllers`、`../models`、`../services`、`../utils` 等目录的源文件
- 因为是在 `add_subdirectory(test)` 子目录中，需要 `unset()` 清除父 CMake 已定义的同名变量，避免路径冲突
- 创建 `calcite_test` 可执行文件，包含 `test_main.cc` 及各业务模块源文件
- 直接链接父目录已 `find_package` 的 `Drogon::Drogon`、`CURL::libcurl`、`OpenSSL::SSL/Crypto`
- 设置包含目录指向父目录及 `models`

构建结果：
- 可执行文件位于 `./calcite/build/test/calcite_test`
- 编译、链接均通过
````