你是一名资深的vue3前端开发人员. 请认真完成如下任务.
## 需求
你需要:
1. 重构搜索结果界面`./calcite-web/src/components/sidebar/SearchResults.vue` 
2. 重构右侧工具栏`./calcite-web/src/components/sidebar/RightSidebar.vue`中笔记信息界面. 
3. 实现其他人的公开笔记预览界面.

## 实现细节与说明
### 搜索结果界面
1. 当前项目中搜索功能对于搜索自己笔记/搜索公开笔记已做了处理, 但搜索结果共用一个界面.
2. 可以通过如下方法直接区分出是否为搜索公开笔记:
	- 返回结果数组中每个元素包含"author_id"字段, 若该字段与用户id相同则一定全部是用户自己的笔记, 否则全部是公开笔记.
3. 你需要为公开笔记搜索结果编写一个新的页面, 同样为组件样式. 
4. 界面行为:当用户点击其中笔记时触发`view`操作, 调用`/api/note/view`这个API. 打开*其他人的公开笔记预览界面*(待实现).

### 右侧工具栏
修改笔记信息面板
- 在右侧工具栏(RightSidebar.vue)中, 修改note-info-panel行为
- 当`/api/note/detail`附带的author_id与userId不同时, 采用*预览笔记信息面板*(需要新建或者在note-info-panel上重构), 否则使用现有的note-info-panel
- *预览笔记信息面板*, 显示如下信息:
	- 笔记标题
	- 点赞总数, 收藏总数
	- 摘要, 但不可修改
	- 创建时间, 修改时间
	- 标签
	- author_id

### 其他人的公开笔记预览界面
对于其他人的公开笔记, 做一个预览界面.
如何区分为公开笔记:
- 当点击笔记时, 后端的`/api/note/detail`现在会附带一个`author_id`字段, 若`author_id`与存储的id(也就是当前用户id)不同就是其他人的公开笔记

对于公开笔记预览界面实现
- 参考NoteEditor.vue L:1~45. 
- 顶部栏将标题栏的el-input修改为不可修改的文本显示 
- 顶部栏将header-actions替换为点赞/收藏按钮. 可调用API:`/api/note/like`,`/api/note/collect`, 以及相应的取消点赞/收藏 API.
- 对于内容渲染, 依旧使用md-editor渲染, 但是只渲染预览界面, 不可编辑, 没有编辑工具栏.

## API参考文档

后端API有如下内容:

| 接口                   | 方法     | 说明   | ./docs/api.md文档具体位置 |
| -------------------- | ------ | ---- | ------------------- |
| `/api/note/search`   | GET    | 搜索笔记 | L:360~403           |
|                      |        |      |                     |
| `/api/note/view`     | POST   | 浏览笔记 | L:465~496           |
| `/api/note/like`     | POST   | 点赞笔记 | L:497~531           |
| `/api/note/collect`  | POST   | 收藏笔记 | L:532~566           |
| `/api/notes/like`    | DELETE | 取消点赞 | L:567~596           |
| `/api/notes/collect` | DELETE | 取消收藏 | L:597~626           |


## 输出
1. 列好你的TODO, 认真按照TODO实现
2. 完成重构后, 在./docs中形成以260421_为前缀的文档

````markdown title:"马马虎虎"
任务已全部完成，构建验证通过。以下是修改概览：

---

### ✅ TODO 完成情况

1. **重构 `SearchResults.vue`**  
   - 新增 `userId` prop，通过 `results[0].author_id !== userId` 自动判定是否为公开笔记搜索。
   - 公开搜索结果标题显示为"公开笔记搜索结果"，列表项额外展示作者 ID。
   - 公开笔记点击时 emit `public-note-click`，由 `Home.vue` 处理并调用 `/api/note/view`。

2. **重构 `RightSidebar.vue` 笔记信息面板**  
   - 新增 `isPreview` prop。当为 `true` 时渲染只读预览面板。
   - 预览面板展示：标题、点赞/收藏总数、摘要（只读）、创建/修改时间、标签、作者 ID。
   - 自己的笔记仍保留原有的编辑面板（公开开关、摘要输入、文件夹选择、删除等）。

3. **实现 `PublicNotePreview.vue`（新建）**  
   - 参考 `NoteEditor.vue` 顶部栏布局：返回按钮 + 不可编辑标题文本 + 点赞/收藏按钮（带计数）。
   - 内容区使用 `md-editor`，设置 `preview-only=true`、`toolbars=[]`，实现纯预览无工具栏。
   - 内部封装点赞/收藏/取消 API，操作成功后通过 `update:note` 同步状态，右侧栏计数实时刷新。

4. **修改 `Home.vue` 与 `api/note.js`**  
   - `Home.vue` 新增 `previewingNote` 状态，笔记点击时通过 `author_id` 分流到预览或编辑。
   - 条件渲染优先级：`SearchResults` → `NoteListView` → `PublicNotePreview` → `NoteEditor`。
   - `api/note.js` 新增 `viewNote`、`likeNote`、`collectNote`、`unlikeNote`、`uncollectNote`。

5. **文档**  
   - 已输出 `./docs/260421_public_note_preview.md`，包含完整变更清单、实现细节、API 对接与数据约定。

---

### 📁 关键文件变更

| 文件 | 操作 |
|------|------|
| `calcite-web/src/api/note.js` | 新增 5 个公开笔记相关 API |
| `calcite-web/src/views/Home.vue` | 集成预览状态与点击分流 |
| `calcite-web/src/components/sidebar/SearchResults.vue` | 支持公开/个人搜索结果区分 |
| `calcite-web/src/components/sidebar/RightSidebar.vue` | 新增预览信息面板 |
| `calcite-web/src/components/center/PublicNotePreview.vue` | **新建** 公开笔记预览组件 |
| `docs/260421_public_note_preview.md` | **新建** 技术文档 |

````



