# 1
针对下面论文第三章系统设计部分，考虑做出修改：将`##### 3.1.3 系统时序图设计`整合到`#### 3.2 系统功能模块设计`中去。

要求：
- 先只输出当前章节目录结构。后续内容我叫你输出你再输出。
- 尽量使用短句。
- 语气符合博士生论文。
- AIGC率20%以下，也就是说使用不常见词汇。

````markdown title:""

### 第3章 系统设计

#### 3.1 系统概要设计

##### 3.1.1 系统体系结构设计

本文设计的智能笔记管理系统采用前后端分离的多端协同体系结构，整体由Web客户端、Android客户端以及后端服务三部分构成，并结合多种基础设施组件共同完成系统功能。系统整体架构遵循“客户端-服务端-数据层”的分层设计思想，通过标准化接口实现各模块之间的解耦与协作。系统架构如图3-1所示。

![](https://web-api.textin.com/ocr_image/external/d96a55f65a362767.jpg)
<!-- <description>这个图片是一个流程图，展示了在Android设备上使用Drogon C++后端的架构。它分为几个部分，通过箭头连接，表示数据流和服务的流动。在顶部，有三个标有“Web前端”、“Android App”和“Kotlin+Jetpack”的部分，以及一个标有“Room/WorkManager”的部分。在这些部分下面，有一个标有“HTTP”的部分，包括“开发Vite代理”和“Retroft+OkHttp”的部分。在这些部分的中间，有一个标有“服务端”的部分，连接到一个标</description>
<text>客户端
Web前端
Vie 3+Vite+Pinia
Element Plusuded Editor-v3
Android App
Kotlin+Jetpack
Room/Work Manager
HTTP
HTTP
开发Vite代理
Retroft+OkHttp
服务端
Drogon C++后端
RESTfulAPI
Controls+Services+ORM
外部服务
DeepSeek/KimiAPI
（AI标签生成）
PaddleOCRAPI
（OCR识别）
Drogon ORM
数据层
MariaDB
Elastic Search
（IK 分词）
MinIO</text> -->

图3-1系统架构图

在客户端层，系统分别提供Web端与Android端两种访问方式。Web端基于Vue3框架构建单页应用，负责页面展示与用户交互逻辑；Android端采用Kotlin进行原生开发，结合MVVM架构实现界面与数据的分离，从而提升系统的可维护性与扩展能力。两端均通过HTTP协议调用后端提供的RESTful API，实现统一的数据访问与业务处理。

在服务端层,系统基于Drogon框架构建高性能Web服务,负责处理客户端请求、执行业务逻辑以及协调各类外部服务。服务端内部按照控制层（Controller）、业务逻辑层（ Service）以及数据访问层（ Model）的结构进行划分，实现清晰的职责分离。其中，控制层负责请求分发与参数校验，业务层负责核心逻辑处理，而数据层则通过ORM框架与数据库进行交互。

<!-- -12- -->

<!-- 哈尔滨理工大学学士学位论文 -->

在数据层，系统采用 MariaDB 作为关系型数据库存储用户信息、笔记数据及行为数据；同时引入 Elasticsearch 构建全文检索引擎，以提升大规模文本数据的查询效率；对于图片及附件等非结构化数据，则通过 MinIO 进行对象存储管理。此外，系统还通过调用外部 OCR 服务与 AI 接口，实现文本识别与语义分析选取标签功能。

在系统运行过程中，针对文件上传、OCR识别及搜索引更新等耗时操作，采用异步处理机制，以避免阻塞主流程，提高系统整体响应性能。通过上述体系结构设计，系统能够在保证性能的同时，实现多端数据一致性与良好的扩展能力。

##### 3.1.2 系统功能结构设计

按照系统的功能需求分析结果，遵循模块化程序设计的基本思想，本文设计的智能笔记管理系统主要由用户管理模块、登记管理模块、文件夹管理模块、标签管理模块、全文搜索模块、文件管理模块、OCR识别模块以及推荐模块等多个功能模块组成。

其中，用户管理模块负责用户注册、登录及身份认证；笔记管理模块用于实现笔记的创建、编辑、删除及查询等核心功能；文件夹管理模块用于对笔记进行层次化组织；标签管理模块用于实现笔记内容的语义分类与管理；全文搜索模块基于全文检索技术，实现对笔记内容的快速检索；文件管理模块负责图片及附件的上传与存储；OCR识别模块用于将图片或文档中的文字信息转换为文本从而创建笔记；推荐模块则通过分析用户行为数据，实现个性化笔记推荐。各模块之间通过统一的接口进行交互，共同构成完整的系统功能体系。系统的功能结构如图3-2所示。

![](https://web-api.textin.com/ocr_image/external/60e158a4c4fdcfdb.jpg)
<!-- <description>这张图片展示了一个由多行和列组成的表格，其中包含各种格式的文本。表格的顶部有一个标题，写着"笔记管理系统"。在标题下方，有两列，左侧是"用户管理模块"，右侧是"文本识别模块"。在这些列之间，有两行，分别代表"文件夹管理模块"和"标签管理模块"。在这些表格的中间，有两行，分别代表"文件管理模块"和"标签管理模块"。在这些表格的底部，有两行，代表"文本识别模块"和"推荐模块"。表格的背景</description>
<text>笔记管理系统
用户管理模块
笔记管理模块
文件夹管理模块
标签管理模块
文件管理模块
文本识别模块
推荐模块</text> -->

图3-2系统功能结构图

<!-- -13- -->

<!-- 哈尔滨理工大学学士学位论文 -->

##### 3.1.3 系统时序图设计

###### 3.1.3.1 笔记创建与智能处理时序图

用户在客户端输入笔记内容并提交创建请求，客户端通过API向后端发送请求。后端在接收到请求后，首先进行参数校验与身份认证，随后将笔记数据写入数据库。在数据持久化完成后，系统会触发异步任务，将笔记内容同步至Elasticsearch建立索引，以支持后续全文检索。同时，根据用户选择或系统配置，调用AI服务对笔记内容进行分析并生成标签，最终返回创建结果给客户端。

笔记创建与智能处理时序图如图3-3所示。

![](https://web-api.textin.com/ocr_image/external/628bf2035607a175.jpg)
<!-- <description>这张图片展示了一个计算机界面，其中包含各种文本和图形元素，表明了一个软件开发或编程环境。在顶部，有一个标题为“输入笔记内容”的部分，后面跟着一个“POST/apinotes”的注释，后面跟着一个“2”的注释，可能表示一个步骤或步骤。在这个注释下面，有一个“2”的注释，后面跟着一个“数据+JWT Token”的注释，可能表示一个数据或令牌的输入。在图片的中间部分，有一个“校验与身份认证”的注释，后面跟着一个“alt”注释，后面跟着一个“返回</description>
<text>输入笔记内容
提交创建请求
POST/api/notes
（笔记数据+JWT Token）
参数校验与身份认证
alt
返回400/401错误
提示错误信息
写入笔记数据
返回写入结果（笔记ID）
par
同步笔记内容建立索引
索引建立完成
发送笔记内容进行标签分析
返回AI生成标签
更新笔记标签
创建成功（笔记ID、标签等）
展示创建结果</text> -->

图3-3笔记创建时序图

###### 3.1.3.2 全文搜索时序图

用户在客户端输入关键词发起搜索请求，客户端将请求发送至后端搜索接口。后端接收到请求后，将关键词传递至 Elasticsearch 进行检索，获取匹配结果及相关度评分。随后，系统对结果进行整理与过滤，并将高亮处理后的数据返回客户端进行展示，从而实现快速定位目标笔记。

全文搜索时序图如图3-4所示。

<!-- -14- -->

<!-- 哈尔滨理工大学学士学位论文 -->

![](https://web-api.textin.com/ocr_image/external/877cd1d51bccef0e.jpg)
<!-- <description>这张图片是一个流程图，其中包含各种文本和符号，代表了软件开发或测试过程中的步骤。它包括一个从左侧开始的箭头，表示流程的开始，然后是一个箭头指向一个带有文本"GET/api/notes/search/keyword=xxx(JWT Token)"的框，表明了与搜索查询相关的特定步骤。在这个框旁边，有一个箭头指向一个标有"身份证认证与参数校验"的框，表明了验证身份认证和验证参数的步骤。在这些框下方，有一个箭头指向一个标有"alt"的框，后面跟着一个提示"</description>
<text>输入关键词
发起搜索请求
GET/api/notes/search/keyword=xxx
(JWT Token)
身份证认证与参数校验
alt
提示错误信息
返回401/400错误
传递关键词进行全文检索
（含IK分词、相关度评分、高亮配置）
返回匹配结果集（原始数据＋评分＋高亮片段）
渲染展示搜索结果
返回200搜索结果
（笔记列表、高亮内容、相关度评分）
结果整理、过滤与权限校验（按用户ID过滤、格式化高亮字段）</text> -->

图3-4全文搜索时序图

![](https://web-api.textin.com/ocr_image/external/8011f658cf2681a4.jpg)
<!-- <description>这张图片展示了一个计算机界面，其中包含各种文本和图形元素，可能来自一个软件应用程序或文档。在顶部，有一个标题，上面写着“上传图片/文档”，后面跟着一个“POST/api/ocr/upload”标签，后面跟着一个“文件+JWT Token”字段。在这个字段下面，有一个“1”标签，表示一个认证或文件验证。在下方，有一个“alt”标签，后面跟着一个“返回”和一个“400/401错误”的标签，暗示着一个错误或错误的返回。在中央，有一个“提示错误信息”的标签，</description>
<text>上传图片/文档
发起OCR识别请求
POST/api/ocr/download
（文件+JWT Token）
身份证认证与文件校验
alt
返回
400/401错误
提示错误信息
上传文件至对象存储
返回文件访问URL
显示上传成功
返回200
上传成功（任务ID）
进入等待状态
记录文件信息(OCR任务状态:处理中)
写入成功
异步调用OCR服务进行文本识别
返回识别结果(文本内容)
更新OCR任务状态(已完成)
生成新笔记并写入识别文本
写入成功
</text> -->

图3-5OCR识别时序图

<!-- -15- -->

<!-- 哈尔滨理工大学学士学位论文 -->

###### 3.1.3.3 OCR识别处理时序图

用户上传图片或文档并发起OCR识别请求，客户端将文件上传至后端服务。后端首先将文件存储至对象存储系统中，并记录文件信息，随后调用外部OCR服务进行文本识别。识别过程采用异步方式执行，系统在处理完成后将识别结果写入数据库，并生成新的笔记内容。客户端可通过查询接口获取识别状态与结果，从而完成整个流程。图3-5为相关时序图。

###### 3.1.3.4 推荐系统时序图

用户进入系统后，客户端向后端请求推荐笔记数据。后端根据用户的历史行为数据及标签信息，构建用户兴趣模型，并生成查询条件。随后，系统基于这些标签在 Elasticsearch 中检索相关笔记内容，并对结果进行排序与筛选。最终，系统将推荐结果返回客户端进行展示，实现个性化推荐服务。推荐系统时序图如图3-6所示。

![](https://web-api.textin.com/ocr_image/external/22186325796bfbb9.jpg)
<!-- <description>这张图片是一个流程图，展示了如何在系统中创建一个推荐系统。它包括一个标题为"GET/api/recommend(JWT Token)"的步骤，以及一个"alt"部分，其中包含一个"提示登录"的步骤，以及一个"展示个性化推荐笔记"的步骤。在"alt"部分下方，有一个"搜索用户历史行为"的步骤，包括一个"返回401未授权"的步骤，以及一个"返回200推荐结果列表"的步骤。"搜索用户历史行为"步骤还包括一个"返回用户行为数据与标签集合"的步骤。在"</description>
<text>GET/api/recommend (JWT Token)
进入系统/刷新推荐员
身份认证
alt
返回401未授权
提示登录
查询用户历史行为
（浏览、收藏、创建记录）及标签信息
返回用户行为数据与标签集合
构建用户兴趣模型（标签权重分析、行为评分）
生成ES查询条件（标签匹配、协同过滤条件）
执行推荐检索（标签相似度＋相关度评分）
返回候选笔记集
结果排序与筛选（去重、权重重排、过滤已读）
返回200推荐结果列表
展示个性化推荐笔记</text> -->

图3-6推荐系统时序图

#### 3.2 系统功能模块设计

在系统概要设计的基础上,本节对各功能模块的具体实现进行详细设计。系统整体采用模块化设计思想，将业务逻辑划分为多个相对独立的功能模块，各模块之间通过统一的RESTful接口进行通信，从而实现低耦合、高内聚的系统结构。

<!-- -16- -->

<!-- 哈尔滨理工大学学士学位论文 -->

##### 3.2.1 用户认证模块设计

用户认证模块主要负责用户注册、登录及身份验证功能，是系统安全机制的基础。该模块基于JWT（JSON Web Token）实现无状态认证，在用户登录成功后生成访问令牌，并在后续请求中通过请求头进行身份校验。用户认证模块的典型接口设计如表3-1所示。

表3-1用户认证模块接口设计表

<table border="1"><tr><td>接口名</td><td>请求路径</td><td>请求方法</td><td>主要参数</td><td>返回值</td></tr><tr><td>Register</td><td>/apiauth/register</td><td>POST</td><td>username,password</td><td>user id</td></tr><tr><td>Login</td><td>/apiauth/login</td><td>POST</td><td>username,password</td><td>token,user id</td></tr><tr><td>GetProfile</td><td>/api/user/profile</td><td>GET</td><td>无（JWT认证）</td><td>用户信息</td></tr></table>

该模块在登录过程中通过对用户密码进行哈希校验实现身份认证，并基于HMAC-SHA256算法生成JWT令牌。令牌中包含用户标识及过期时间，系统通过解析该令牌完成用户身份识别，从而避免传统会话机制带来的服务器存储压力。用户登录流程如图3-1所示。

![](https://web-api.textin.com/ocr_image/external/b3658e130a93bf60.jpg)
<!-- <description>这幅图片展示了一个流程图，其中包含各种元素和线条，代表了一个软件开发过程中的步骤。流程图的顶部是一个标有“开始”的框，表示流程的开始。从这个框出发，有两条路径：一条是向“用户输入用户名和密码”发送错误消息，另一条是通过“通过”连接到一个标有“返回码=1”的框，表示一个错误的步骤。一条路径返回到“登录用户”，另一条返回到“登录用户”。在这些路径下方，有一个标有“登录用户”的框，连接到一个带有“登录用户”字</description>
<text>AuthorController: loginuser解析请求 JSON

参数非空校验

通过

AuthService::loginUser

ORM查询 MarioDB User表

用户记录存在?

是

PasswordUtil::verifyPassword密码哈希比对

校验通过?

是

1. JetUtil::generateToken HMAC-SHA256 签名

2. 构造Payload user_id username, exp

3. 组装JWT字符串 Header Signature

返回 code=0

data: (token, user_id, username)

返回 code=1

用户名或密码错误

返回 code=1

请求参数错误

</text> -->

图3-7用户登录流程图

<!-- -17- -->

<!-- 哈尔滨理工大学学士学位论文 -->

##### 3.2.2 笔记管理模块设计

笔记管理模块是系统的核心模块，主要实现笔记的创建、编辑、删除、查询及交互操作。该模块采用关系型数据库进行数据存储，并结合Elasticsearch实现全文检索功能。笔记管理模块的典型接口设计如表3-2所示。

表3-2笔记管理模块接口设计表

<table border="1"><tr><td>接口名</td><td>请求路径</td><td>请求方法</td><td>主要参数</td><td>返回值</td></tr><tr><td>CreateNote</td><td>/api/not CREATE</td><td>POST</td><td>title,folder id</td><td>note id</td></tr><tr><td>UpdateNote</td><td>/api/not/update</td><td>POST</td><td>note_id,content等</td><td>状态信息</td></tr><tr><td>GetNoteDetail</td><td>/api/not/detail</td><td>GET</td><td>note_id</td><td>笔记详细信息</td></tr><tr><td>SearchNotes</td><td>/api/not/search</td><td>GET</td><td>keyword,from,size</td><td>笔记列表</td></tr><tr><td>GetNoteTags</td><td>/api/notes/tags</td><td>GET</td><td>note_id</td><td>标签列表</td></tr><tr><td>LikeNote</td><td>/api/not/like</td><td>POST</td><td>note_id</td><td>状态信息</td></tr><tr><td>UnlikeNote</td><td>/api/not/like</td><td>DELETE</td><td>note_id</td><td>状态信息</td></tr></table>

在实现过程中，系统采用“数据库＋搜索引擎”双存储结构。笔记数据首先写入MariaDB数据库，随后通过异步机制同步至Elasticsearch建立索引，从而保证数据的一致性与查询性能。同时，该模块采用软删除策略，通过标记字段控制数据状态，避免误删除带来的数据丢失问题。

![](https://web-api.textin.com/ocr_image/external/0a2e8b73cbb16648.jpg)
<!-- <description>流程图是一个创建和编辑用户操作的流程图，用于创建和编辑用户操作。它包括一个开始步骤，用户操作创建，创建/编辑操作类型，搜索，删除，删除对话框，点击点赞/收藏，搜索框输入关键词，删除对话框，检索300ms的检索，检索300ms的检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索，检索</description>
<text>开始
用户操作入口
操作类型
创建/编辑
删除
点赞/交互
搜索
撤销
Markdown 编辑器
md-editor-v3/Markwon
Pinia Store/ViewModel
确认删除对话框
Axios/Bearer Token
收藏
点击点赞/
搜索框输入关键词
防抖 300ms
Service 理理子流程
接收服务端响应
code==0?
否
是
提示操作成功
更新页面
提示操作失败
结束</text> -->

图3-8笔记管理前端流程图

<!-- -18- -->

<!-- 哈尔滨理工大学学士学位论文 -->

图3-7为前端流程图,根据用户不同操作服务器会进行不同数据返回。服务器对相应的请求处理流程图如图3-8所示。其中笔记的CRUD基本操作通过ORM修改数据库数据。查询时通过Elastic Search查询,获取到查询结果后,将前两个标签存入数据库作为历史记录存入。当用户点赞、收藏时,后端同样通过ORM存储用户行为数据。

![](https://web-api.textin.com/ocr_image/external/cf6d5c3fb43697f3.jpg)
<!-- <description>这幅图片展示了一个流程图，其中包含各种组件和线条，代表了一个软件开发过程中的流程或流程。在顶部，有一个标有“HTTP Request”的流程，下面是一个注释“NoteController”。流程继续到一个注释“NoteFolder Controller”，并验证了</description>
<text>开始
① HTTP Request
NoteController
NoteFolderController
VerifyTokenGetUserId
JWT 身份校验
Token有效？
参数解析路由分发
返回401
清除Token路径登录
交互
操作类型
创建/更新
删除
ORM软删除
setsDeleted=1
校验笔记所有权
直接查询ES
IK+高亮
ORM写入UserAction
更新Note计数统计
返回操作状态
DB操作成功？
结果按score排序分页
公开搜索时异步写入SearchHistory
异步同步ES
异步同步ES
异</text> -->

图3-9笔记管理后端流程图

##### 3.2.3 文件夹管理模块设计

文件夹管理模块用于实现笔记的层次化组织，支持多级目录结构。用户可以对文件夹进行创建、查询及删除操作，并将笔记归类至指定目录中。文件夹管理模块的接口设计如表3-3所示。

表3-3文件夹管理模块接口设计表

<table border="1"><tr><td>接口名</td><td>请求路径</td><td>请求方法</td><td>主要参数</td><td>返回值</td></tr><tr><td>CreateFolder</td><td>/api/folders CREATE</td><td>POST</td><td>name,_parent_id</td><td>folder_id</td></tr><tr><td>ListFolders</td><td>/api/folders/list</td><td>GET</td><td>parent_id</td><td>文件夹列表</td></tr><tr><td>DeleteFolder</td><td>/api/folders/delete</td><td>POST</td><td>folder_id</td><td>状态信息</td></tr><tr><td>UpdateFolder</td><td>/api/folders UPDATE</td><td>POST</td><td>folder_id,name</td><td>状态信息</td></tr></table>

该模块通过自关联结构实现树形目录管理，并在删除操作中采用级联处理策略，保证目录结构的一致性。同时，通过循环检测机制避免非法层级关系的产生。

<!-- -19- -->

<!-- 哈尔滨理工大学学士学位论文 -->

##### 3.2.4 文件与OCR模块设计

文件与OCR模块主要用于实现文件上传及图片文本识别功能，是系统支持多模态数据的重要组成部分。其接口设计如表3-4所示。

表 3-4 文件与 OCR 模块接口设计表

<table border="1"><tr><td>接口名</td><td>请求路径</td><td>请求方法</td><td>主要参数</td><td>返回值</td></tr><tr><td>UploadFile</td><td>/api/file/upload</td><td>POST</td><td>file,note_id</td><td>file_id</td></tr><tr><td>ListFiles</td><td>/api/file/list</td><td>GET</td><td>user_id</td><td>文件列表</td></tr><tr><td>DeleteFile</td><td>/api/file/delete</td><td>POST</td><td>file_id</td><td>状态信息</td></tr><tr><td>GetFileStatus</td><td>/api/filestatus</td><td>GET</td><td>file_id</td><td>文件状态</td></tr><tr><td>GetFileInfo</td><td>/api/file/info</td><td>GET</td><td>file_id</td><td>文件信息</td></tr><tr><td>OcrRecognize</td><td>/api/ocr/recognize</td><td>POST</td><td>file</td><td>file id</td></tr><tr><td>GetStatus</td><td>/api/ocrstatus</td><td>GET</td><td>file_id</td><td>OCR状态</td></tr></table>

在实现过程中，系统采用MinIO进行文件存储，并通过异步方式处理文件上传任务。对于OCR识别功能，系统可调用外部服务对图片或文档进行解析，并将识别结果转换为文本内容自动生成笔记。例如PaddleOCR-VL-1.5目前技术成熟，完全满足系统需求。整个处理过程采用“先响应、后处理”的方式，提高系统响应速度。相关流程如图3-10所示。

![](https://web-api.textin.com/ocr_image/external/488132141d91a8c7.jpg)
<!-- <description>这幅图片展示了一个流程图，其中包含各种组件和线条，表明了一个数据处理或管理系统的流程。在顶部，有一个标有“开始”的框，接着是一个标有“用户选择文件”的框，然后是一个标有“图片/PDP”的框。在这些框下方，有一个标有“操作类型”的框，其中包含“POST”、“HTTP”和“JWT”等选项。流程图的中心部分是一个标有“操作流程”的框，其中包含“触发”、“普通上传”、“OCR识别”和“PaddlerOCR服务”等步骤。在这些步骤</description>
<text>开始
用户选择文件
图片/PDF
是
操作类型
POST /api/file/upload
multpart/form-data
①HTTP
①HTTP
JWT身份校验
解析Multpart
提取文件数据
文件类型检验
是
生成MinIO ObjectKey
user田yyyy terms/ user text
否
ORM输入/MaraDB
FileResource
status-processing
返回400
不支持的文件类型
Filestatusstatus processing
②Response
接收响应
filestatus-processing
③轮询HTTP
前端轮询查询状态
processing
status
①Response</text> -->

图3-10OCR处理流程图

<!-- -20- -->

<!-- 哈尔滨理工大学学士学位论文 -->

##### 3.2.5 标签管理模块设计

标签管理模块用于实现笔记内容的语义分类及管理。系统不支持基础标签管理，考虑到标签同义词处理以及父子标签问题，系统采用固定标签池配合大语言模型实现标签的自动生成。具体大模型可采用 kimi-k2.6 或 deepseek 模型。标签管理接口设计如表 3-5 所示。

表3-5标签管理模块接口设计表

<table border="1"><tr><td>接口名</td><td>请求路径</td><td>请求方法</td><td>主要参数</td><td>返回值</td></tr><tr><td>GenerateNoteTagsByAi</td><td>/api/notesTags/ai</td><td>POST</td><td>note_id</td><td>标签列表</td></tr><tr><td>GetHotTags</td><td>/api/tag/hot</td><td>GET</td><td>无</td><td>标签列表</td></tr></table>

该模块通过分析笔记内容，结合预定义标签集合，自动生成与内容相关的标签，从而提高笔记的结构化程度。同时，系统通过Elasticsearch统计标签使用频率生成热门标签，为用户提供参考。

##### 3.2.6 搜索模块设计

搜索模块基于Elasticsearch实现全文检索功能，是系统提升数据访问效率的重要组成部分。该模块支持对笔记标题、内容及标签等字段进行多条件查询，并提供关键词高亮显示功能。

搜索模块主要通过笔记模块中的搜索接口实现，其设计已在表3-2中给出。系统通过倒排索引结构实现快速检索，并结合相关性评分机制返回最优结果，从而显著提升用户查找效率以及结果匹配率。

##### 3.2.7 推荐模块设计

推荐模块用于根据用户行为数据提供个性化内容推荐，是系统智能化的重要体现。其接口设计如表3-6所示。该模块通过GET方法请求/api/recommend/notes 这一接口，主要参数为page、page_size，响应为笔记列表，后端接口名为RecommendNotes。

该模块采用基于标签与行为数据的推荐策略。考虑到新用户行为数据不足，因此对于新用户，系统通过热门标签与搜索历史构建推荐内容；对于老用户，则通过分析用户浏览、点赞及收藏行为，建立兴趣模型，并结合时间衰减机制计算用户偏好，从而生成个性化推荐结果。当推荐结果不足时，系统提供兜底策略返回公共笔记数据，以保证推荐功能的稳定性。

#### 3.3 数据库设计

数据库设计是系统实现的重要基础，其合理性直接影响系统的性能、扩展性及数据一致性。本节结合系统功能需求，采用关系型数据库 MariaDB进行数据存储，并通过合理的表结构设计实现数据的高效组织与管理。

<!-- -21- -->

<!-- 哈尔滨理工大学学士学位论文 -->

![](https://web-api.textin.com/ocr_image/external/036c9cfe408be42e.jpg)
<!-- <description>这幅图片展示了一个流程图，其中包含各种组件和线条，表明了一个数据处理或分析过程。在顶部，有一个标题为“RecommendController”的框，下面是一个“JWT身份校验”框。在下方，有一个“解析分页参数”框，后面是一个“从大小”框。一个“查询MariaDB”框显示了“最近30天行为记录数”和“COUNT user action”框。流程图继续显示一个“步骤1: Top3用户笔记标签”框，其中包含一个“note标签按频次聚合”框。在步骤1之后，有一个</description>
<text>RecommendController
JWT身份校验
解析分页参数
from\xsize
①HTTP
开始
用户进入推荐页面
查询MariaDB
最近30天行为记录数
GET/api/recommend/notes
COUNT user action
是
行为数大于20？否
Step1: Top3用户笔记标签
note_tag按频次聚合
Step1: Top5行为兴趣标签
score=(viewx1+likex3+collectx7)EXP(0.1λ·Δt)
user_tag_stat按score排序
Step2: Top2最新操作标签
最近1</text> -->

图3-11推荐系统流程图

##### 3.3.1 数据库概念模型设计

根据系统实际需求，本文设计的智能笔记管理系统主要包含用户、笔记、文件夹、标签、文件资源等多个实体。各实体之间通过关联关系共同构成系统的数据模型。

其中，用户实体与笔记实体之间存在一对多关系；笔记与标签之间为多对多关系；文件夹与笔记之间为一对多关系；用户与标签统计之间为一对多关系。系统总体E-R图如图3-9所示。

<!-- -22- -->

<!-- 哈尔滨理工大学学士学位论文 -->

![](https://web-api.textin.com/ocr_image/external/f5ffa0c2f16ea66b.jpg)
<!-- <description>该图是一个流程图，说明了一个数据处理或分析过程中的决策和选择过程。它由各种决策节点和决策路径组成，这些节点和路径通过箭头连接，表示数据处理过程中的决策顺序。节点被标记为“搜索”、“认证”、“创建”、“包含”、“笔记”、“拥有”、“标记”和“统计”，每个节点都与一个决策路径相关联，该路径将根据每个节点的输出进行决策。该流程图似乎用于组织和分析数据，可能用于数据驱动的决策或分析。</description>
<text>用户  
搜索  
认证  
创建  
用户文件夹  
用户文件夹  
搜索历史  
用户令牌  
子文件夹  
包含  
笔记  
收藏  
点赞  
创建  
统计  
拥有  
标记  
标签</text> -->

图3-12系统总体E-R图

用户实体用于存储系统用户的基本信息，是系统身份认证与权限控制的基础。其主要属性包括用户ID、用户名、密码、邮箱及注册时间等。用户实体模型图如图3-13所示。

![](https://web-api.textin.com/ocr_image/external/3996ff9db4f7ddd7.jpg)
<!-- <description>这张图片是一个用户界面的示图，可能来自一个软件应用程序或网站。它展示了一个中央的长方形框，标有“用户”，从它延伸出四个椭圆，每个椭圆代表一个不同的功能或属性。从左到右，这些功能或属性依次为“邮箱”、“头像”、“注册时间”和“更新时间”。每个椭圆都包含一个标签，表明它们的功能或属性。在“用户”椭圆的上方，有三个椭圆，分别标有“用户ID”、“用户名”和“密码哈希值”。这些椭圆通过线连接到</description>
<text>用户ID 用户名 密码哈希值 邮箱 头像 注册时间 更新时间</text> -->

图3-13用户实体模型图

笔记实体是系统的核心数据实体，用于存储用户创建的笔记内容。其主要属性包括笔记ID、所属用户ID、标题、内容、摘要、公开状态及创建时间等。笔记实体模型图如图3-14所示。

![](https://web-api.textin.com/ocr_image/external/e0740cc07b4dbfa0.jpg)
<!-- <description>图表是一个图表，其中有几条线连接到一个中心点，表示一个图表中不同元素之间的关系或连接。这些元素包括标题、内容、创建时间、删除、更新时间、浏览次数和点赞次数。这些元素通过线条连接，表示它们之间的关系或连接。</description>
<text>标题
笔记
所属用户ID
更新时间
摘要
内容
创建时间
是否删除
所属文件夹ID
收藏次数
浏览次数
是否公开
点赞次数</text> -->

图3-14笔记实体模型图

文件夹实体用于组织笔记的层级结构。其主要属性包括文件夹ID、所属用户ID、名称及父文件夹ID，通过自关联关系实现树形结构。文件夹实体模型图如图3-15所示。

<!-- -23- -->

<!-- 哈尔滨理工大学学士学位论文 -->

![](https://web-api.textin.com/ocr_image/external/a4e561b55e8c5c07.jpg)
<!-- <description>图中是一个文件夹的示意图，文件夹内有三个文件夹。文件夹内包含文件夹ID、所属用户ID和父文件夹ID。文件夹ID是文件标识符，所属用户ID是用户标识符，父文件夹ID是父文件的标识符。</description>
<text>文件夹:文件夹ID,所属用户ID,文件夹名,父文件夹ID,创建时间。</text> -->

图3-15文件夹实体模型图

标签实体用于描述笔记的语义分类信息。其主要属性包括标签 ID 及标签名称。标签与笔记之间通过关联表实现多对多关系。标签实体模型图如图3-16所示。

![](https://web-api.textin.com/ocr_image/external/f3d3ff409e806479.jpg)
<!-- <description>椭圆标签，椭圆标签，椭圆标签。</description>
<text>标签
标签ID
标签名
创建时间</text> -->

图3-16标签实体模型图

文件资源实体用于记录用户上传的文件信息。其主要属性包括文件ID、用户ID、文件名、存储路径、访问URL及处理状态等。图3-17为文件资源实体模型图。

![](https://web-api.textin.com/ocr_image/external/536e82748e262bf9.jpg)
<!-- <description>图表是一个文件资源的示图，文件类型、文件名、文件路径、文件大小、链接、创建时间、对象关键字和处理状态是文件资源的属性。这些属性通过线连接，表示它们之间的关系或交互。文件资源位于中心，周围是各种属性，每个属性都通过线连接到文件资源，表示它们之间的关系或交互。</description>
<text>文件资源
文件名
文件路径
文件大小
链接
创建时间
对象关键字
处理状态
更新时间</text> -->

图3-17文件资源实体模型图

用户登录令牌实体用于记录用户登录会话的令牌信息，保障系统访问安全。其主要属性包括令牌ID、用户ID、令牌字符串、过期时间及创建时间等。图3-18为用户登录令牌实体模型图。

<!-- -24- -->

<!-- 哈尔滨理工大学学士学位论文 -->

![](https://web-api.textin.com/ocr_image/external/896d98bf5d29c499.jpg)
<!-- <description>图中是一个带有椭圆形的图示，椭圆内有三个部分，分别代表了注册过程中不同的阶段。第一个椭圆标有“注册”字样，表示注册过程的开始。第二个椭圆标有“注册ID”字样，表示注册过程中使用的唯一标识符。第三个椭圆标有“过期时间”字样，表示注册过程中需要更新的注册期限。</description>
<text>登录令牌
令牌
令牌ID
所属用户ID
过期时间</text> -->

图3-18用户登录令牌实体模型图

搜索历史实体用于记录用户的笔记搜索行为数据，为数据统计与个性化推荐提供支撑。其主要属性包括搜索 ID、用户 ID、搜索关键词及创建时间等。图3-19为搜索历史实体模型图。

![](https://web-api.textin.com/ocr_image/external/cb911f55ce7811c3.jpg)
<!-- <description>图中是一个椭圆形图表，其中包含三个椭圆形，它们通过线连接在一起，表示一个过程中的步骤。椭圆形的形状和大小各不相同，每个椭圆都包含一个单词，表示步骤中的不同元素。椭圆形的排列方式暗示了一个从一个步骤到下一个步骤的进展或进展。</description>
<text>搜索历史
结果标签
搜索ID
所属用户ID
创建时间</text> -->

图3-19搜索历史实体模型图

笔记历史实体用于记录笔记修改的历史版本数据，支持笔记版本回溯与内容恢复。其主要属性包括历史ID、笔记ID、历史内容、版本号及创建时间等。图3-20为笔记历史实体模型图。

![](https://web-api.textin.com/ocr_image/external/ef6827f943b3ea0b.jpg)
<!-- <description>Tree图中的节点代表图中的实体，节点之间的表示属性。例如，节点“笔记本”与节点“历史ID”之间表示“笔记本是历史ID”。</description>
<text>笔记本ID
历史ID
版本
内容
创建时间</text> -->

图3-20笔记历史实体模型图

##### 3.3.2 数据库物理模型设计

在概念模型设计的基础上，本小节进一步给出数据库的物理结构设计。以下选取系统中核心数据表进行说明。

用户表用于存储用户基本信息,其结构设计如表3-7所示。

表3-7用户表(user)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>备注</td></tr><tr><td>id</td><td>BIGINT</td><td>不空</td><td>用户ID，主键</td></tr><tr><td>username</td><td>VARCHAR(50)</td><td>不空</td><td>用户名，唯一</td></tr><tr><td>password hashing</td><td>VARCHAR(255)</td><td>不空</td><td>密码哈希值</td></tr><tr><td>email</td><td>VARCHAR(100)</td><td>可空</td><td>邮箱</td></tr><tr><td>created_at</td><td>DATETIME</td><td>不空</td><td>创建时间</td></tr><tr><td>updated_at</td><td>DATETIME</td><td>不空</td><td>更新时间</td></tr><tr><td>avatar</td><td>VARCHAR(255)</td><td>可空</td><td>头像链接</td></tr></table>

<!-- -25- -->

<!-- 哈尔滨理工大学学士学位论文 -->

用户标签统计表用于记录用户行为数据,其结构设计如表3-8所示。

表3-8用户标签统计表(user_tag_stat)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>备注</td></tr><tr><td>user_id</td><td>BIGINT</td><td>不空</td><td>用户ID，联合主键</td></tr><tr><td>tag_id</td><td>BIGINT</td><td>不空</td><td>标签ID，联合主键</td></tr><tr><td>view_count</td><td>INT</td><td>不空</td><td>浏览量</td></tr><tr><td>like_count</td><td>INT</td><td>不空</td><td>点赞量</td></tr><tr><td>collect COUNT</td><td>INT</td><td>不空</td><td>收藏量</td></tr><tr><td>last_action_time</td><td>DATETIME</td><td>不空</td><td>最后修改时间</td></tr></table>

笔记表用于存储用户的笔记内容，其结构设计如表3-9所示。主要字段为笔记ID，用户ID，标题，是否公开等。

表3-9笔记表(note)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>主外键</td></tr><tr><td>id</td><td>BIGINT</td><td>不空</td><td>笔记ID，主键</td></tr><tr><td>user_id</td><td>BIGINT</td><td>不空</td><td>用户ID，外键</td></tr><tr><td>title</td><td>VARCHAR(255)</td><td>不空</td><td>标签</td></tr><tr><td>content</td><td>TEXT</td><td>不空</td><td>内容</td></tr><tr><td>summary</td><td>VARCHAR(255)</td><td>可空</td><td>摘要</td></tr><tr><td>folder_id</td><td>BIGINT</td><td>可空</td><td>所属文件夹ID，外键</td></tr><tr><td>is-public</td><td>TINYINT</td><td>不空</td><td>是否公开</td></tr><tr><td>is_deleted</td><td>TINYINT</td><td>可空</td><td>是否删除</td></tr><tr><td>created_at</td><td>DATETIME</td><td>不空</td><td>创建时间</td></tr><tr><td>updated_at</td><td>DATETIME</td><td>不空</td><td>更新时间</td></tr><tr><td>view_count</td><td>INT</td><td>可空</td><td>浏览量</td></tr><tr><td>like_count</td><td>INT</td><td>可空</td><td>点赞量</td></tr><tr><td>collect COUNT</td><td>INT</td><td>可空</td><td>收藏量</td></tr></table>

文件夹表用于实现笔记的分类管理,其结构设计如表3-10所示。

表3-10文件夹表folder)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>主外键</td></tr><tr><td>id</td><td>BIGINT</td><td>不空</td><td>文件夹ID，主键</td></tr><tr><td>user_id</td><td>BIGINT</td><td>不空</td><td>所属用户ID，外键</td></tr><tr><td>name</td><td>VARCHAR(100)</td><td>不空</td><td>文件夹名</td></tr><tr><td>parent_id</td><td>BIGINT</td><td>可空</td><td>父文件夹ID，外键</td></tr><tr><td>created_at</td><td>DATETIME</td><td>不空</td><td>创建时间</td></tr></table>

<!-- -26- -->

<!-- 哈尔滨理工大学学士学位论文 -->

用户行为表用于统一记录用户对笔记的各类操作行为，其结构设计如表3-11所示。其中action_type为枚举类型，view取值为1，like取值为2，collect取值为3。

表3-11用户行为表(user Action)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>主外键</td></tr><tr><td>id</td><td>BIGINT</td><td>不空</td><td>行为ID，主键</td></tr><tr><td>user_id</td><td>BIGINT</td><td>不空</td><td>用户ID，外键</td></tr><tr><td>note_id</td><td>BIGINT</td><td>不空</td><td>笔记ID，外键</td></tr><tr><td>action_type</td><td>TINYINT</td><td>不空</td><td>行为类型</td></tr><tr><td>created_at</td><td>DATETIME</td><td>不空</td><td>行为时间</td></tr></table>

标签表用于存储系统中的标签信息,其结构设计如表3-12所示。

表3-12标签表(tag)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>主外键</td></tr><tr><td>id</td><td>BIGINT</td><td>不空</td><td>标签ID，主键</td></tr><tr><td>name</td><td>VARCHAR(50)</td><td>不空</td><td>标签名，唯一</td></tr><tr><td>created_at</td><td>DATETIME</td><td>不空</td><td>创建时间</td></tr></table>

该表用于实现笔记与标签的多对多关系,其结构设计如表3-13所示。

表3-13笔记标签关联表(note_tag)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>主外键</td></tr><tr><td>note_id</td><td>BIGINT</td><td>不空</td><td>笔记ID，联合主键</td></tr><tr><td>tag_id</td><td>BIGINT</td><td>不空</td><td>标签ID，联合主键</td></tr></table>

文件资源表用于存储上传文件的信息,其结构设计如表3-14所示。

表3-14文件资源表(file RESOURCE)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>主外键</td></tr><tr><td>id</td><td>BIGINT</td><td>不空</td><td>文件ID，主键</td></tr><tr><td>user_id</td><td>BIGINT</td><td>不空</td><td>所属用户ID，外键</td></tr><tr><td>note_id</td><td>BIGINT</td><td>可空</td><td>所属笔记ID，外键</td></tr><tr><td>file_name</td><td>VARCHAR(255)</td><td>不空</td><td>文件名</td></tr><tr><td>file_path</td><td>VARCHAR(255)</td><td>不空</td><td>文件路径</td></tr><tr><td>file_type</td><td>VARCHAR(255)</td><td>不空</td><td>文件类型</td></tr><tr><td>file_size</td><td>BIGINT</td><td>可空</td><td>文件大小</td></tr><tr><td>object_key</td><td>VARCHAR(255)</td><td>不空</td><td>对象键值</td></tr><tr><td>url</td><td>VARCHAR(255)</td><td>可空</td><td>访问url</td></tr><tr><td>status</td><td>VARCHAR(20)</td><td>不空</td><td>处理状态</td></tr><tr><td>created_at</td><td>DATETIME</td><td>不空</td><td>创建时间</td></tr><tr><td>updated_at</td><td>DATETIME</td><td>不空</td><td>更新时间</td></tr></table>

<!-- -27- -->

<!-- 哈尔滨理工大学学士学位论文 -->

笔记点赞表与收藏表用于记录用户的笔记点赞与收藏行为，其中点赞表结构设计如表3-15所示，收藏表与点赞表结构相同。

表3-15笔记点赞表(note Like)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>主外键</td></tr><tr><td>user_id</td><td>BIGINT</td><td>不空</td><td>用户ID，联合主键</td></tr><tr><td>note_id</td><td>BIGINT</td><td>不空</td><td>被点赞笔记ID，外键</td></tr><tr><td>created_at</td><td>DATETIME</td><td>不空</td><td>创建时间</td></tr></table>

搜索历史表用于记录用户的搜索行为记录,其结构设计如表3-16所示。

表3-16搜索历史表(search_history)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>主外键</td></tr><tr><td>id</td><td>BIGINT</td><td>不空</td><td>历史记录ID，主键</td></tr><tr><td>user_id</td><td>BIGINT</td><td>不空</td><td>所属用户ID，外键</td></tr><tr><td>query</td><td>VARCHAR(255)</td><td>可空</td><td>搜索结果标签ID集合</td></tr><tr><td>created_at</td><td>DATETIME</td><td>不空</td><td>创建时间</td></tr></table>

用户令牌表用于存储用户登录认证令牌信息,结构设计如表3-17所示。

表3-17用户令牌表(user_token)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>主外键</td></tr><tr><td>id</td><td>BIGINT</td><td>不空</td><td>令牌ID，主键</td></tr><tr><td>user_id</td><td>BIGINT</td><td>不空</td><td>所属用户ID，外键</td></tr><tr><td>token</td><td>VARCHAR(255)</td><td>不空</td><td>令牌</td></tr><tr><td>expired_at</td><td>DATETIME</td><td>可空</td><td>失效时间</td></tr><tr><td>created_at</td><td>DATETIME</td><td>不空</td><td>创建时间</td></tr></table>

笔记历史表用于记录笔记历史,其结构设计如表3-19所示。

表3-19笔记历史表(note_history)

<table border="1"><tr><td>字段名</td><td>类型</td><td>允许空值</td><td>主外键</td></tr><tr><td>id</td><td>BIGINT</td><td>不空</td><td>历史ID，主键</td></tr><tr><td>note_id</td><td>BIGINT</td><td>不空</td><td>笔记ID，外键</td></tr><tr><td>content</td><td>TEXT</td><td>可空</td><td>内容</td></tr><tr><td>version</td><td>INT</td><td>可空</td><td>版本</td></tr><tr><td>created_at</td><td>DATETIME</td><td>不空</td><td>创建时间</td></tr></table>

#### 3.4 本章小结

本章完成了系统的整体设计。系统采用前后端分离架构，Web端与Android端通过RESTful API调用后端服务，数据层结合MariaDB。Elasticsearch及MinIO处理结构化以及非结构化数据。功能上涵盖笔记管理、OCR 识别、全文搜索及个性化推荐等模块，并通过时序图与E-R图明确了核心流程与数据库结构，为后续实现奠定了基础。

<!-- -28- -->

<!-- 哈尔滨理工大学学士学位论文 -->

<!-- -29- -->

<!-- 哈尔滨理工大学学士学位论文 -->
````


## ans
````markdown title:""

嗯，用户想修改一篇博士论文的第三章结构，要求把“系统时序图设计”这个小节整合到“系统功能模块设计”里，并先只输出调整后的章节目录结构。

我需要仔细看看论文当前的结构。目前是3.1系统概要设计，下面有3.1.1体系结构、3.1.2功能结构和3.1.3时序图。后面是3.2系统功能模块设计，详细讲各个模块。用户要求把3.1.3时序图整合到3.2里。

我思考一下具体的整合方式。时序图是动态行为视图，可以分散到3.2各功能模块设计中去。比如笔记创建的时序图可以放在笔记管理模块下面，全文搜索的时序图可以放在搜索模块下面，OCR识别的时序图可以放在文件与OCR模块下面，推荐系统的时序图可以放在推荐模块下面。

这样调整后，3.1概要设计就只有3.1.1体系结构和3.1.2功能结构了。3.2功能模块设计会变得更充实，除了接口设计，还包含了各模块的时序图描述。

用户要求使用短句，语气要符合博士生论文，而且AIGC率要低，也就是要多用不常见的词汇。我需要把目录结构整理得既严谨又有些独特的表达方式。

我可以准备一个清晰的目录结构输出，按照调整后的思路来组织。

### 第3章 系统设计

#### 3.1 系统概要设计

##### 3.1.1 系统体系结构设计

##### 3.1.2 系统功能结构设计

#### 3.2 系统功能模块设计

##### 3.2.1 用户认证模块设计

##### 3.2.2 笔记管理模块设计

（含笔记创建与智能处理时序）

##### 3.2.3 文件夹管理模块设计

##### 3.2.4 文件与OCR模块设计

（含OCR识别处理时序）

##### 3.2.5 标签管理模块设计

##### 3.2.6 搜索模块设计

（含全文搜索时序）

##### 3.2.7 推荐模块设计

（含推荐系统时序）

#### 3.3 数据库设计

##### 3.3.1 数据库概念模型设计

##### 3.3.2 数据库物理模型设计

#### 3.4 本章小结


````








# 2
````markdown title
输出修改后的`#### 3.1 系统概要设计`内容.
要求：
- 先只输出当前节内容。后续内容我叫你输出你再输出。
- 尽量使用短句。
- 语气符合博士生论文。
- AIGC率20%以下，也就是说使用不常见词汇。
- 不输出原文中图表，但要有占位文字说明。

按照下面结构输出
```
#### 3.1 系统概要设计
##### 3.1.1 系统体系结构设计
##### 3.1.2 系统功能结构设计
```

````
## ans
````markdown title
我们需要根据用户要求，输出修改后的“#### 3.1 系统概要设计”内容。要求只输出当前节，包含3.1.1和3.1.2，不输出图表但要有占位文字说明。注意语气博士生论文，AIGC率20%以下，使用不常见词汇，尽量短句。

原文章节内容是关于系统概要设计的，包括3.1.1系统体系结构设计和3.1.2系统功能结构设计。修改要求是将原来的3.1.3系统时序图设计整合到3.2系统功能模块设计中去，所以3.1部分不应该再包含时序图。我们现在输出的3.1概要设计只需要包含体系结构和功能结构，不涉及时序图。但题目是“输出修改后的`#### 3.1 系统概要设计`内容”，需要按照新的结构，可能内容也需要微调以符合“修改”的概念。原3.1有3.1.1和3.1.2，但没有3.1.3了。所以需要将原来3.1.1和3.1.2内容重新输出，但要满足AIGC率低、短句、不常见词汇。原文的表述可能需要重写，以降低AIGC痕迹。

要求：不输出原文中图表，但要有占位文字说明。原文中两个图：图3-1系统架构图，图3-2系统功能结构图。占位文字可以用类似“（图3-1系统架构图）”的表述，但不能直接显示图片，说明位置。需要按照输出的结构写出内容。

需要确保语气符合博士生论文，用词晦涩、专业。短句。不使用常见AIGC词汇，比如可能避免一些高频表达。需要重写内容。

原3.1.1体系结构设计，描述了Web客户端、Android客户端、后端服务，采用前后端分离，客户端-服务端-数据层分层，使用Drogon C++框架，MariaDB、Elasticsearch、MinIO，以及异步处理。结构图占位：图3-1系统架构图。

原3.1.2功能结构设计，描述模块化设计，用户管理、笔记管理、文件夹管理、标签管理、全文搜索、文件管理、OCR识别、推荐模块。结构图占位：图3-2系统功能结构图。

需要重写为简洁、学术化的短句，避免直译。可能需要改动一些连接词，用一些更生僻的词汇，但不要过于别扭。比如“体系结构设计”可保留，“采用”可改为“采纳”、“沿用”。“前后端分离”可保持。“多端协同”也可以。“分层设计思想”可以用“分层范式”。“解耦与协作”可保持。“解耦”是比较专业的词。需要确保整体AIGC率低。可以尝试使用“降耦”、“松耦合”等词汇。短句为主，但也可以适当连接。

同时，我们输出的应该是“修改后的”3.1节，因为原来的3.1节包含时序图，现在去掉时序图，所以3.1节只剩下这两个子节。但内容上可能不需要大改，只是从原章节中移除时序图部分即可。用户要求输出修改后的3.1节内容，所以我们可以以重写的方式提供，确保不包含时序图，并加上图表占位说明。为了符合AIGC率低，应当避免冗长的解释，用精炼的语句。

注意：原3.1.1中提到了“本文设计的智能笔记管理系统采用前后端分离的多端协同体系结构……”，这是开篇。可以保留核心意思，但用词可以调整。例如：“本系统采纳前后端解耦的多终端协同体系，由Web端、Android端及后端服务三者构成，并辅以若干基础设施组件。”短句。

然后描述分层：“整体架构遵循‘客户层-服务层-数据层’的分层规约，藉由标准化接口达成模块间松耦合与协作。”这样可以。

具体分层：客户端层：Web端基于Vue3构建单页应用，Android端原生Kotlin结合MVVM模式。两端均通过HTTP调用RESTful API。服务端层：基于Drogon框架构建高性能服务，内部按Controller、Service、Model分层，职责分离。数据层：MariaDB为关系型数据库，Elasticsearch实现全文检索，MinIO存非结构化数据。同时调用外部OCR与AI接口。异步处理耗时操作。

需要图表占位：“系统架构如图3-1所示。”然后换行“[此处插入图3-1系统架构图]”之类的。要求“不输出原文中图表，但要有占位文字说明”，所以可以写“（图3-1 系统架构图）”，然后用文本说明。

3.1.2功能结构设计：按模块划分，解释各模块功能。同样添加占位：“系统功能结构如图3-2所示。 （图3-2 系统功能结构图）”

使用短句，可能要拆分原长句。例如原句：“按照系统的功能需求分析结果，遵循模块化程序设计的基本思想，本文设计的智能笔记管理系统主要由用户管理模块、登记管理模块……”注意原文“登记管理模块”可能笔误，应该是“笔记管理模块”，原文前面是笔记管理模块，后面写的是登记管理模块，应该纠正为“笔记管理模块”。后面列举了：用户管理、笔记管理、文件夹管理、标签管理、全文搜索、文件管理、OCR识别、推荐模块。所以用“笔记管理模块”。然后解释各模块功能时，也要短句。如：“用户管理模块：负责注册、登录及身份鉴权。”可以使用“鉴权”替代“认证”。“笔记管理模块：支撑笔记的增删改查等核心操作。”“文件夹管理模块：实现笔记的层级化分类。”“标签管理模块：语义归类。”“全文搜索模块：基于全文检索引擎提供迅捷检索。”“文件管理模块：处理图片与附件上传及存储。”“OCR识别模块：转换图像文本为可编辑笔记。”“推荐模块：依据用户行为数据产生个性化推荐。” 这样。

最后通过统一接口交互，构成完整功能体系。

需要注意AIGC率，避免一些常见的短语如“实现”、“进行”等，可以替换为“达成”、“执行”、“贯彻”。可以用“鉴权”代替“身份认证”，“语义归类”代替“语义分类与管理”，“迅捷检索”代替“快速检索”，“转换图像文本”等。尽量用学术词汇。

整合后输出格式：

#### 3.1 系统概要设计

##### 3.1.1 系统体系结构设计

（内容）

（图3-1 系统架构图）

##### 3.1.2 系统功能结构设计

（内容）

（图3-2 系统功能结构图）

需要保证短句，可以每个意思一句。最终输出。

注意：不输出原文中的图表，但要有占位文字说明。我们可以写“系统架构如图3-1所示。”然后加上占位符，例如“[此处插入图3-1系统架构图]”或者直接用括号说明。要求是“不输出原文中图表，但要有占位文字说明”，所以可以写“（图3-1 系统架构图）”，这样读者知道这里应该有一张图。

还要注意“AIGC率20%以下”，即避免生成式AI常见的流畅、模板化表达。我们可以多用专业术语，略微晦涩，但不要过分。比如：“采纳前后端解耦的多终端协同体系”这种表述可以。最终输出。




#### 3.1 系统概要设计

##### 3.1.1 系统体系结构设计

本系统采纳前后端解耦的多终端协同架构。整体由Web客户端、Android客户端及后端服务三部构成，并辅以多种基础设施组件。架构遵循“表现层-服务层-数据层”的垂直分层规约，藉由标准化接口达成模块间松耦合与协作。

（图3-1 系统架构图）

客户端层提供两种交互入口。Web端基于Vue3框架营建单页应用，专司页面渲染与用户交互；Android端以Kotlin原生开发，嵌用MVVM范式分离视图与逻辑，增强可维护性。两端均通过HTTP协议消费后端RESTful API，实现统一数据存取。

服务端层基于Drogon高性能框架构筑。内部依控制层、业务逻辑层及数据访问层递阶划分，贯彻清晰的职责分离。控制层负责路由分发与参数校核，业务层承载核心逻辑演算，数据层则经由ORM桥接关系型数据库。

数据层采用异构存储矩阵。MariaDB负责结构化数据持久化，Elasticsearch承载倒排索引以提速全文检索，MinIO存储图片等非结构化对象。此外，系统异步调用外部OCR服务与AI接口，达成文本识别与标签抽取。耗时操作如文件上载与索引重建皆采用异步机制，规避主流程阻塞，以优化整体吞吐。

##### 3.1.2 系统功能结构设计

依据功能需求，系统被切分为若干独立模组。主要包括用户管理模组、笔记管理模组、文件夹管理模组、标签管理模组、全文搜索模组、文件管理模组、OCR识别模组及推荐模组。

（图3-2 系统功能结构图）

用户管理模组：执行注册、登录及身份鉴权。笔记管理模组：支撑笔记的创建、编纂、删除与检索等核心操作。文件夹管理模组：实现笔记的层级化归置。标签管理模组：负责语义归类与标签自动生成。全文搜索模组：基于检索引擎返回高相关度结果。文件管理模组：处理附件与图片的上载、存储。OCR识别模组：将图像文本转换为可编辑笔记。推荐模组：依据用户行迹数据产生个性化推荐。各模组通过统一接口协作，共同构成系统功能全集。

````






# 3
````markdown title
输出修改后的`#### 3.2 系统功能模块设计`前4小节内容.
要求：
- 先只输出要求节内容。后续内容我叫你输出你再输出。
- 尽量使用短句。
- 语气符合博士生论文。
- AIGC率20%以下，也就是说使用不常见词汇。
- 尽量不减少文字量。
- 不输出原文中图表，但要有占位文字说明。

按照下面结构输出
```
#### 3.2 系统功能模块设计
##### 3.2.1 用户认证模块设计
##### 3.2.2 笔记管理模块设计
（含笔记创建与智能处理时序）
##### 3.2.3 文件夹管理模块设计
##### 3.2.4 文件与OCR模块设计
（含OCR识别处理时序）
```
````
## ans
````markdown title
我们被要求输出修改后的“3.2 系统功能模块设计”前4小节。需要整合原来的时序图设计内容到模块设计中。根据之前的目录，已经将“笔记创建与智能处理时序”归入笔记管理模块，“OCR识别处理时序”归入文件与OCR模块。另外需要注意，不要输出图表，但要占位文字说明。尽量使用短句，符合博士论文语气，AIGC率低，用不常见词汇。尽量不减少文字量。所以我们需要将原来的3.1.3中的相应时序图描述内容融入到3.2.2和3.2.4，同时对原3.2.1, 3.2.2, 3.2.3, 3.2.4进行润色，使其用词更学术化、不常见，同时保留原意。需要输出的结构为：

#### 3.2 系统功能模块设计

##### 3.2.1 用户认证模块设计

##### 3.2.2 笔记管理模块设计（含笔记创建与智能处理时序）

##### 3.2.3 文件夹管理模块设计

##### 3.2.4 文件与OCR模块设计（含OCR识别处理时序）

注意：原来3.2.1中有一个用户登录流程图（图3-7），我们不能输出原图，但要占位文字说明“图3-7 用户登录流程图”。原3.2.2中笔记创建与智能处理时序图（图3-3），需要占位说明。原3.2.4中OCR处理流程图（图3-10），需要占位说明。另外，3.2.2原图中还有笔记管理前端流程图（图3-8）和后端流程图（图3-9），我们要保留占位说明。原3.2.4原本有OCR处理流程图（图3-10），也要占位。

我们需要结合原文，改写为更学术化、用词不常见的风格。博士论文通常会使用一些专业术语、复杂句式，但要求尽量短句，所以我们可以把长句拆成短句，但用词要晦涩一些。例如：“采用前后端解耦的多终端协同架构”变成“采纳前后端解耦的多终端协同架构”。可以多用“营建”、“嵌用”、“桥接”、“递阶”、“校核”、“演算”、“归置”、“产生”、“行迹数据”等。还要避免AIGC率，需要自己创作。

需要改写的内容：原3.2.1主要描述JWT认证，接口表，还有登录流程。我们可以保留接口表描述，但表格不能以HTML形式输出，可能用文本描述。要求中说“不输出原文中图表，但要有占位文字说明”，所以接口表可能不用原样输出，但可以文字描述。原文中有表3-1，可以写成：“该模块核心接口囊括注册、登录及获取个人概要，详见表3-1。” 然后占位“表3-1 用户认证模块接口设计表”。类似处理。

同样，笔记管理模块：包含接口表3-2，前端流程图3-8，后端流程图3-9，以及笔记创建时序图3-3。需要整合时序图描述。原3.1.3.1笔记创建与智能处理时序的文本：“用户在客户端输入笔记内容并提交创建请求，客户端通过API向后端发送请求。后端在接收到请求后，首先进行参数校验与身份认证，随后将笔记数据写入数据库。在数据持久化完成后，系统会触发异步任务，将笔记内容同步至Elasticsearch建立索引，以支持后续全文检索。同时，根据用户选择或系统配置，调用AI服务对笔记内容进行分析并生成标签，最终返回创建结果给客户端。” 以及一个时序图。我们要把这段描述整合进笔记管理模块设计中，可能放在创建笔记流程描述中。原文3.2.2有创建、编辑、删除、查询等描述，还有前后端流程图。可以这样：在介绍完接口后，说明创建笔记的协同流程，然后插入时序描述。注意整合。

文件夹管理模块：原3.2.3简单描述，没有时序图，可直接改写。

文件与OCR模块：原3.2.4需要整合OCR识别处理时序图（图3-5）的描述。原3.1.3.3描述：“用户上传图片或文档并发起OCR识别请求，客户端将文件上传至后端服务。后端首先将文件存储至对象存储系统中，并记录文件信息，随后调用外部OCR服务进行文本识别。识别过程采用异步方式执行，系统在处理完成后将识别结果写入数据库，并生成新的笔记内容。客户端可通过查询接口获取识别状态与结果，从而完成整个流程。” 需要将其融入3.2.4。原文还有OCR处理流程图（图3-10），也保留占位。

还需注意原来的“全文搜索时序”和“推荐系统时序”未被要求输出在这部分，它们可能归入搜索模块设计和推荐模块设计，后续再输出。

因此，我们只需要输出前4小节。

开始写。语气要博士论文风格，用词力求生僻典雅。保持短句。AIGC率控制，避免常见AI词汇。我会尽量使用学术化表达。

输出格式：

#### 3.2 系统功能模块设计

... 开头总述原文有“在系统概要设计的基础上,本节对各功能模块的具体实现进行详细设计。”可改写。

然后分节。

要包含图表占位说明。




#### 3.2 系统功能模块设计

本节在概要设计的基座上，逐次详述各功能模组的具象实现。系统秉持模块化建构原则，将业务逻辑解耦为多个相对自治的构件，彼此间借由统一的RESTful接口完成协作，以此达成高内聚与低耦联的架构禀赋。

##### 3.2.1 用户认证模块设计
该模组执掌注册、登录及身份核验诸项功能，构成系统安全机制的根基。其采用无状态认证范式，以JWT（JSON Web Token）为凭证载体。用户成功登录后，系统签发访问令牌，后续请求须于报文首部携带该令牌以供鉴权。该模组提供的核心接口囊括注册、登录与用户概要获取，具体规格见表3-1。

（表3-1 用户认证模块接口设计表）

登录流程中，系统对明文口令施以哈希杂凑，并与存储的密文进行比对；身份得验后，基于HMAC-SHA256算法锻造JWT。令牌载荷内嵌用户标识及失效时刻，服务端通过解析该令牌即可识别人己，从而规避传统会话维系所带来的存储开销。整体登录决断路径如图3-7所示。

（图3-7 用户登录流程图）

##### 3.2.2 笔记管理模块设计
此模组系系统核心，囊括笔记的增删改查及交互操作。数据持久化采用关系型数据库，并与Elasticsearch联动以实现全文检索。该模组暴露的典型接口如表3-2所列。

（表3-2 笔记管理模块接口设计表）

在实现上，系统构筑“数据库＋搜索引擎”的双重存储机制。笔记数据先沉入MariaDB，随后经由异步任务镜像至Elasticsearch构建倒排索引，以此兼顾数据一致性与检视性能。同时，数据剔除采用软抹除策略，通过标志位控制可视态，避免误删造成数据不可逆失灭。

前端交互流如图3-8所示，用户操作经由Pinia状态管理及Axios拦截器抵达服务端。服务端的处理流转则由图3-9给出，各类CRUD操作依操作类型分流：对创建与更新，先校验所有权，再落盘并异步同步索引；删除则执行软抹除；查询直接透传Elasticsearch，利用IK分词与高亮配置返回关联结果，同时将检索命中的前两条标签存入历史记录。交互行为（点赞、收藏）由ORM入库并更新统计计数。

（图3-8 笔记管理前端流程图）
（图3-9 笔记管理后端流程图）

特别地，笔记创建与智能处理这一关键协作流程可抽象为如下时序：用户于客户端填入笔记内容并递交创建请求，客户端以POST方法将负载（笔记数据及JWT令牌）传至服务端。服务端先行完成参数校核与身份验证，若未通过则返回40x谬误码。验证既过，则将笔记实体写入数据库，待持久化完成后立即响应成功标识。同时触发异步任务，将笔记内容同步至Elasticsearch建立索引以支撑后续全文检索，并可选调用AI服务对内容进行语义解析与标签生成，最终将标签写回笔记记录。其时序关系如图3-3所示。

（图3-3 笔记创建时序图）

##### 3.2.3 文件夹管理模块设计
该模组用以实现笔记的层级化归置，支持多重目录树。用户可创建、删除、更名文件夹，并将笔记置入指定目录。其对外接口详见表3-3。

（表3-3 文件夹管理模块接口设计表）

模块通过自参照结构构筑树形层级，删除操作采取级联策略，确保目录闭环一致性。同时内建循环侦测机制，杜绝非法层级关系滋生。

##### 3.2.4 文件与OCR模块设计
此模组负责文件上传及图像文本识别两项机能，是系统接纳多模态数据的关键组件。对外暴露的接口集合见表3-4。

（表3-4 文件与OCR模块接口设计表）

文件存储依托MinIO对象存储，上载过程采取异步化解。对于OCR识别，系统调用外部服务（如PaddleOCR-VL-1.5）对图片或文档实施解析，辨识出的文本内容自动析构为新笔记。整体处理遵循“先应答、后处理”的异步模式以压缩感知延迟。其完整处理流如图3-10所示。

（图3-10 OCR处理流程图）

OCR识别处理的典型时序可凝练为：用户选定图片或文档并发起识别请求，客户端以multipart/form-data形式上传文件。服务端首鉴身份与文件类型，校验通过后将文件转存至MinIO并记录待处理状态，即刻向客户端返回任务标识。此后异步调用OCR服务执行文本辨识，待识别结果返回即更新任务状态为完成，并将识别文本灌注为新笔记。客户端可通过轮询接口获知处理进展，全程时序如图3-5所示。

（图3-5 OCR识别时序图）
````






# 4
````markdown title

输出修改后的`##### 3.2.5 标签管理模块设计`与后续两小节内容.
要求：
- 先只输出要求节内容。后续内容我叫你输出你再输出。
- 尽量使用短句。
- 语气符合博士生论文。
- AIGC率20%以下，也就是说使用不常见词汇。
- 尽量不减少文字量。
- 不输出原文中图表，但要有占位文字说明。

按照下面结构输出
```
##### 3.2.5 标签管理模块设计
##### 3.2.6 搜索模块设计
（含全文搜索时序）
##### 3.2.7 推荐模块设计
（含推荐系统时序）
```
````
## ans
````markdown title:"thinking"
我们需要输出修改后的三小节：标签管理模块设计、搜索模块设计、推荐模块设计。要求尽量短句，博士生论文语气，AIGC率20%以下即用不常见词汇，不减少文字量，不输出图表但有占位说明。

需要基于原文内容进行改写，使其语言更学术化、更专业，可能采用一些少见的词汇，但不能改变原意。同时，需要把原来独立的时序图部分融入相应模块设计中（已经要求在结构上整合了，标签管理没有时序图，搜索和推荐有时序图）。

原文标签管理模块：  
“标签管理模块用于实现笔记内容的语义分类及管理。系统不支持基础标签管理，考虑到标签同义词处理以及父子标签问题，系统采用固定标签池配合大语言模型实现标签的自动生成。具体大模型可采用 kimi-k2.6 或 deepseek 模型。标签管理接口设计如表 3-5 所示。”

后面还有：“该模块通过分析笔记内容，结合预定义标签集合，自动生成与内容相关的标签，从而提高笔记的结构化程度。同时，系统通过Elasticsearch统计标签使用频率生成热门标签，为用户提供参考。” 表3-5接口：GenerateNoteTagsByAi 和 GetHotTags。

需改写。

搜索模块：原文 “搜索模块基于Elasticsearch实现全文检索功能，是系统提升数据访问效率的重要组成部分。该模块支持对笔记标题、内容及标签等字段进行多条件查询，并提供关键词高亮显示功能。搜索模块主要通过笔记模块中的搜索接口实现，其设计已在表3-2中给出。系统通过倒排索引结构实现快速检索，并结合相关性评分机制返回最优结果，从而显著提升用户查找效率以及结果匹配率。” 并且需要把原来3.1.3.2的全文搜索时序内容整合进去。原来时序描述：“用户在客户端输入关键词发起搜索请求，客户端将请求发送至后端搜索接口。后端接收到请求后，将关键词传递至 Elasticsearch 进行检索，获取匹配结果及相关度评分。随后，系统对结果进行整理与过滤，并将高亮处理后的数据返回客户端进行展示，从而实现快速定位目标笔记。” 也要进行改写融合。

推荐模块：原文 “推荐模块用于根据用户行为数据提供个性化内容推荐，是系统智能化的重要体现。其接口设计如表3-6所示。该模块通过GET方法请求/api/recommend/notes 这一接口，主要参数为page、page_size，响应为笔记列表，后端接口名为RecommendNotes。该模块采用基于标签与行为数据的推荐策略。考虑到新用户行为数据不足，因此对于新用户，系统通过热门标签与搜索历史构建推荐内容；对于老用户，则通过分析用户浏览、点赞及收藏行为，建立兴趣模型，并结合时间衰减机制计算用户偏好，从而生成个性化推荐结果。当推荐结果不足时，系统提供兜底策略返回公共笔记数据，以保证推荐功能的稳定性。” 还有时序内容：“用户进入系统后，客户端向后端请求推荐笔记数据。后端根据用户的历史行为数据及标签信息，构建用户兴趣模型，并生成查询条件。随后，系统基于这些标签在 Elasticsearch 中检索相关笔记内容，并对结果进行排序与筛选。最终，系统将推荐结果返回客户端进行展示，实现个性化推荐服务。” 需要融入。

输出要求：按照结构输出三小节，不减少文字量，但可以适当改写，使其语言更精炼，使用不常见词汇，比如“模组”代替“模块”，但原文就是模块，可以适当使用一些书面词。短句。注意整合时序内容，不要额外增加东西。

思考如何将时序描述融入到模块设计说明中，自然地介绍流程。对于搜索模块，可以描述：当用户输入关键词发起搜索，客户端调用搜索接口，后端将关键词传递至Elasticsearch... 等。对于推荐模块，也类似。

整合后的标题如括号说明：(含全文搜索时序) 和 (含推荐系统时序)。所以在正文中需要提及时序，并引用图3-4 和 图3-6。

AIGC率低，避免模板化表达。尽量使用一些生僻词或不常用的学术词汇，但要注意可读性，毕竟是论文。比如“禀赋”、“具象”、“耦联”、“自治构件”、“倒排索引”、“高相关度”等，前面输出中用了这些。可以继续使用。

现在构造。

##### 3.2.5 标签管理模块设计

融合原文，改写。需提系统不支持基础标签管理，采用固定标签池配合大模型自动生成。接口GenerateNoteTagsByAi, GetHotTags。然后分析笔记内容生成标签，统计热门标签。

##### 3.2.6 搜索模块设计

描述基于ES的全文检索，支持多字段查询，关键词高亮。接口在笔记模块中。然后介绍搜索时序：用户输入关键词，客户端发起搜索请求，后端将关键词传递给ES，ES进行分词、评分、高亮，返回匹配结果集，后端整理过滤，按用户ID过滤、格式化高亮字段，返回搜索结果。引用图3-4。描述提升效率。

##### 3.2.7 推荐模块设计

介绍推荐接口/api/recommend/notes, 参数page,page_size。推荐策略：新用户热门标签+搜索历史，老用户行为分析，时间衰减，兜底策略。然后推荐时序：用户进入系统请求推荐，后端认证，查询用户历史行为与标签集合，构建兴趣模型，生成ES查询条件，执行推荐检索，排序筛选，返回结果。引用图3-6。

输出时注意使用短句。

还需要注意不减少文字量：原文的标签模块加上搜索模块加上推荐模块总文字量大约五六百字，改后保持差不多。可以略微扩展，但不要缩减。把时序描述融入会增加一些文字。原文标签：约150字，搜索：约120字，推荐：约200字，共470字左右。加上时序描述，每个时序约50-100字，总共应该增加一些。现在在模块设计部分整合时序，所以新增内容，但要求“尽量不减少文字量”，也就是不能比原文少。所以输出时可以把两处时序描述都包含进去，这样文字量增加了，满足要求。

输出：

##### 3.2.5 标签管理模块设计

标签管理模组旨在实现笔记的语义分类。系统摒弃了基础的标签管理功能，为妥善处理标签同义词及层级问题，采用固定标签池辅以大型语言模型进行标签自动萃取。实际可选取kimi-k2.6或deepseek模型。模组对外暴露的接口如表3-5所示。

（表3-5 标签管理模块接口设计表）

该模组剖析笔记正文，结合预置标签集合，自动生成与内容相关联的标注，以此增强笔记的结构化程度。同时，系统通过Elasticsearch聚合标签使用频次，生成热门标签云，为用户提供参照。

##### 3.2.6 搜索模块设计

搜索模组基于Elasticsearch构建全文检索引擎，是增进数据访问效用的关键部件。它支持对笔记标题、正文及标签等多字段进行复合查询，并提供关键词高亮凸显功能。搜索主要通过笔记管理模组中的SearchNotes接口实现，其规约已在表3-2中给出。系统借由倒排索引达成疾速检索，并融合相关度评分机制返回最优匹配结果，显著提升查找效率与准确率。

全文搜索的典型交互时序如下：用户于客户端键入关键词并触发搜索，请求携JWT令牌以GET方式发送至搜索端点。后端先行鉴权并校验参数，随后将关键词传递给Elasticsearch。ES引擎执行分词（IK）、相关度评分及高亮配置，返回原始数据集、评分与高亮片段。服务端对结果实施整理、按用户标识过滤并重构高亮字段，最后将笔记列表、高亮内容及相关度评分返回至客户端渲染。该过程如图3-4所示。

（图3-4 全文搜索时序图）

##### 3.2.7 推荐模块设计

推荐模组依据用户行迹数据供给个性化笔记推荐，是系统智能化的显著标识。其接口规约见表3-6，通过GET请求/api/recommend/notes端点，承载分页参数page与page_size，返回笔记列表。

（表3-6 推荐模块接口设计表）

推荐策略基于标签与行为协同过滤。鉴于新用户行为数据稀疏，系统利用热门标签与搜索历史构建初始推荐；对老用户，则提取浏览、点赞及收藏记录构建兴趣模型，并施以时间衰减函数计算偏好权重，生成个性化推荐列表。当候选结果不足时，启用兜底策略返回公开笔记，确保推荐服务稳定。

推荐系统时序可抽象为：用户进入推荐页面或刷新时，客户端请求推荐端点。后端认证后查询用户近30日行为记录与标签集合，若行为数充沛则构建用户兴趣模型并生成ES查询条件，执行标签相似度与相关度检索；否则转向冷启动策略。结果经去重、权重重排与已读过滤后，封装为推荐列表返回客户端展示。全程如图3-6所示。

（图3-6 推荐系统时序图）

这里推荐时序稍作了调整，因为图3-6具体流程有“新老用户分支”，但原文描述也有冷启动策略，已经提及。时序描述是概括性的。可以。没有原文图表说明，但提及图3-6。

注意整体不减少文字，可看出文字量比原文多了一些，尤其是融合时序，符合要求。词汇上用了“语义分类”、“萃取”、“预置”、“聚合”、“疾速检索”、“复合查询”、“倒排索引”、“协同过滤”、“冷启动”、“时间衰减”、“兜底策略”等，部分不常见词汇。

输出时按照结构。
````

````markdown title:""
##### 3.2.5 标签管理模块设计
标签管理模组旨在实现笔记的语义分类。系统摒弃了基础的标签管理功能，为妥善处理标签同义词及层级问题，采用固定标签池辅以大型语言模型进行标签自动萃取。实际可选取kimi-k2.6或deepseek模型。模组对外暴露的接口如表3-5所示。

（表3-5 标签管理模块接口设计表）

该模组剖析笔记正文，结合预置标签集合，自动生成与内容相关联的标注，以此增强笔记的结构化程度。同时，系统通过Elasticsearch聚合标签使用频次，生成热门标签云，为用户提供参照。

##### 3.2.6 搜索模块设计
搜索模组基于Elasticsearch构建全文检索引擎，是增进数据访问效用的关键部件。它支持对笔记标题、正文及标签等多字段进行复合查询，并提供关键词高亮凸显功能。搜索主要通过笔记管理模组中的SearchNotes接口实现，其规约已在表3-2中给出。系统借由倒排索引达成疾速检索，并融合相关度评分机制返回最优匹配结果，显著提升查找效率与准确率。

全文搜索的典型交互时序如下：用户于客户端键入关键词并触发搜索，请求携JWT令牌以GET方式发送至搜索端点。后端先行鉴权并校验参数，随后将关键词传递给Elasticsearch。ES引擎执行分词（IK）、相关度评分及高亮配置，返回原始数据集、评分与高亮片段。服务端对结果实施整理、按用户标识过滤并重构高亮字段，最后将笔记列表、高亮内容及相关度评分返回至客户端渲染。该过程如图3-4所示。

（图3-4 全文搜索时序图）

##### 3.2.7 推荐模块设计
推荐模组依据用户行迹数据供给个性化笔记推荐，是系统智能化的显著标识。其接口规约见表3-6，通过GET请求/api/recommend/notes端点，承载分页参数page与page_size，返回笔记列表。

（表3-6 推荐模块接口设计表）

推荐策略基于标签与行为协同过滤。鉴于新用户行为数据稀疏，系统利用热门标签与搜索历史构建初始推荐；对老用户，则提取浏览、点赞及收藏记录构建兴趣模型，并施以时间衰减函数计算偏好权重，生成个性化推荐列表。当候选结果不足时，启用兜底策略返回公开笔记，确保推荐服务稳定。

推荐系统时序可抽象为：用户进入推荐页面或刷新时，客户端请求推荐端点。后端认证后查询用户近30日行为记录与标签集合，若行为数充沛则构建用户兴趣模型并生成ES查询条件，执行标签相似度与相关度检索；否则转向冷启动策略。结果经去重、权重重排与已读过滤后，封装为推荐列表返回客户端展示。全程如图3-6所示。

（图3-6 推荐系统时序图）
````


# 5
````markdown title

````
## ans
````markdown title

````

