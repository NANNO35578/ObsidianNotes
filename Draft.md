解析太慢
遍历 两个点击事件中的帧
二分法（考虑弹窗）
不能直接按照 抽帧序号\*抽帧间隔/帧率 获取点击时间需要将将 视频帧直接与时间戳映射，直接通过给出的时间戳找；
视频是变频帧率，但给出的是平均帧率
cap = cv2.VideoCapture(path) 打开视频；
直接使用 抽帧号\*帧间隔\*帧率 获得的点击时间发现 点击帧 解析时间比真实的点击时间 靠前一些，总是从上一个 事件开始解析；

计算TTI时发现与实际时间出入过大，发现点击帧 解析时间比真实的点击时间 靠前一些，总是从上一个 事件开始解析；排查发现视频是变频帧率(通过帧的时间戳列表 中相临帧的时间间隔看的)，获取到的帧率是一个平均值，计算使用平均值是不正确的；

后续修改是去获取帧的实际时间戳列表(帧索引号->在视频中的时间位置) ——cap.read()后cap.get(cv2.CAP_PROP_POS_MSEC)获取帧在视频中的精确时间位置，直接使用有效帧对应的实际时间戳与点击时间作差获得TTI ，而不是通过帧率计算时间再作差；

统一输入和输出格式为JSON
解析log完成点击时间戳的替换时，由于用正则匹配config 内容时不能正确识别格式，是由于config 中的内容太多，需要修改为一种更好解析的格式josn；

异常数据 系统性排查（面试官可能追问"系统性"具体指什么方法论？）
异常数据 系统性排查（面试官可能追问"系统性"具体指什么方法论？）
准备排查SOP的具象化描述，比如：
"我建立了四层排查体系：L1 输入（输入事件是否完整或错位/视频录制是否完整/视频是否出现遮挡或延迟/case流程（qc跳转、预搜命中）/ ）→ L2 OCR层（过滤词（一般是新的case 遮挡、新的关键字））→ L3 AI层（提示词版本/温度参数）每层都有checklist和快速验证手段"

TTI 数据 None ：
L1 输入（视频录制是否完整/视频是否出现遮挡/输入事件不完整（AI输出的json格式不完整）/case流程（qc跳转失败）/ ）→ L2 OCR层（过滤词（一般是新的case 遮挡、新的关键字））→ L3 AI层（提示词版本）每层都有checklist和快速验证手段"

偏大：
L1 输入（点击后有延迟（+偏移）/视频本身有网络延迟/预搜未命中）→ L2 OCR层（过滤词（一般是新的case 遮挡））→ L3 AI层（提示词版本）"
偏小：
L1 输入（iOS 加偏移量后可能给出的点击时间已经在有效帧范围内了）
数据混乱：L1 case 流程（cq跳转失败）
目前 还不稳定的原因：
网络延迟、 
发出点击指令后，ai 思考时间不一致；考虑OCR 和文本点击； 
安卓的点击延迟 比 IOS 更高，但为了保证 android的TTI数据的真实性 统一的处理逻辑是 点击事件为，实际点击时间+200ms，但是IOS 是没什么延迟的 会导致提取进入有效界面，导致TTI 极小；因此TTI 统一解析处理的极小的数据会统一+200ms 
了解了一下 是 事件分发机制不一样：事件到达时
android 点击行为给主线程消息队列，主线程处理完当前任务才能分发事件； 
IOS 直接加入主线程 RunLoop，立即处理 
点击时间应该是出现动画时，因为实际点击中没有ai 思考延迟；

ai
接入
更新新模型的时候，采用原来的 RestAPI调用，但是会有很怪的编码和大小写报错导致连接不上模型，一直问 Al也不知道什么原因，所以改了一个连接方式，采用 OpebAI SDK的方式连接
temperature（统一提示词输出是否稳定）：0 几乎确定输出，适合代码/JSON；0.7-1.0 适合创意写作；>1 容易胡言乱语 temperature=0

越高幻觉率越高；
max_tokens（限制输出长度）：设置过短会截断输出；过长浪费成本，模型可能输出分析过程（"这张图片首先...然后...结论..."），破坏 JSON 解析
因为还需要 输出 各case 时间戳事件 因此设置为 500；

ai 幻觉
根据给出的判断条件，幻想自己存在对应内容，判断为有效：OCR先过滤完，再让ai 检测，可以有效避免ai 幻觉和 减少token;

在测试携程国际酒店的流程时发现大模型测试不准确，最后排查发现是由于在给AI的提示词中添加了“国内”的限定，导致干扰，直接在日志中输出AI判断图片无效的原因就能快速定位；

提示词绕过
自动漏掉条件，判断为有效：测试时，可以让ai 输出有效原因；根据ai的输出有效原因修改提示词；

ai 不能正确计算时间
log解析时，一直让大模型计算有效时间戳，一直不准确，更新想法只让大模型识别有效事件，让脚本进行计算有效时间

ai coding
优点
能快速熟悉陌生项目,了解项目结构和目的（如果对一些专有名词不太了解，可以让ai 给一个比较通俗的例子去理解，能快速对一些专业名



下面编程题目使用C++语言编写，不使用万能头。给出精简思路以及解题代码即可。




