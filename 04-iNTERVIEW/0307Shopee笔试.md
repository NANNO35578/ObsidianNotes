### 选择
1. 如果事务T对数据D已加S锁, 这其他事务对数据D()
A. 可以加S锁, 也可以加X锁.
B. 可以加S锁, 不能加X锁.
C. 不能加S锁, 可以加X锁.
D. 不能加任何锁.

2. 要保证在任意连接方式下都能连通具有26个顶点的无向图, 所需要最少的边数
3. 某文件系统的目录由文件名和索引节点号构成. 每个
4. 以下哪个场景, 进程不会进入内核态
5. 
6. 硬币游戏:连续扔硬币, 直到某一人获胜. A获胜条件先正后反, B获胜条件连续出现两次正. A获胜概率()
7. 使用折半查找法查找序列`[1,3,5,7,12,17,45,63,76,79,95]`中依次比较的元素是()
8. 已知小根堆`[8,15,10,21,34,17,11]`, 删除关键字8后需要建堆, 在此基础上需要()次交换
9. 虚拟内存的容量只受()的作用
10. 关于TCP的连接建立, 以下说法错误的是

### 多选
1. 关于SMTP协议的叙述, 正确的是
A. 
B. 
C. 
D. 

2. 某浏览器发出的HTTP请求报文如下
```
GET /index.html HTTP/1.1
Host:www.test.edu.cn
Connection:Close
Cookie:123456
```
则下列说法中, 正确的是:
A. 
B. 
C. 
D. 

3. 下列哪些方案可实现*幂等性*
A. 数据库唯一主键
B. 数据库乐观锁
C. Token令牌
D. 数据库自增ID

4. 关于关系型数据库, 那些说法是错误的
A. 
B. 
C. 
D. 

5. 哪些排序算法时间复杂度为$O(n\times \log_n )$
A. 快速排序
B. 归并排序
C. 冒泡排序
D. 堆排序

### 编程
1. 最小路径和问题(需处理输入)
2. 最长回文子串`{cpp}std::string longestPalindrome(std::string s);`
3. 










