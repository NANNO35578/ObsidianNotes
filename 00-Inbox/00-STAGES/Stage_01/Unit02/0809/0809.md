# `esayX` `C++`图形库初体验


```c
#include <iostream>
#include <graphics.h>


int main() {
	initgraph(800, 600);
	//IMAGE img1; //创建一个IMAGE变量，用以保存加载进内存的图片
	//loadimage(&img1, L".\\fox\\0-0.png");
	//从硬盘任意位置加载图像，并默认图像大小
	//putimage(10, 20, &img1);
	//把 img1 地址的图片呈现到坐标(x,y)值是 10 20 的绘图窗口

	IMAGE images[4][4];

	wchar_t path[30];
	for (int i = 0; i < 4; i++)
		for (int j = 0; j < 4; j++) {
			swprintf_s(path, L".\\fox\\%d-%d.png", i, j);
			loadimage(&images[i][j], path);
		}

	//for (int i = 0; i < 4; i++)	
	for (int x = 100; x < 700; )
		for (int j = 0; j < 4; j++) {
			putimage(x, 200, &images[0][j]);
			Sleep(50);
			x+=10;
		}

	system("pause");
	return 0;
}
```


Next Note : [[0810]]