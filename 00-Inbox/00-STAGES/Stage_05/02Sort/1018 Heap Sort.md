# 堆排序

`leftSon = 2* index + 1;` `rightSon = 2* index + 2` index为待调整节点下标 

> 将数组元素逻辑化为一个完全二叉树 数组元素个数`len` 首地址`arr` 父亲节点下标范围`0 ~ (len/2-1)` 从最后一个父亲节点构造大顶堆 直至第一个节点 
> 此时堆顶为最大值 *此方法可以动态维护最大值* 将堆顶与末尾元素*交换* 标记最后元素下标的变量-- 从堆顶开始调整 调整完 堆顶是最大值 继续交换...

- 无需分析最后元素为左孩子还是右孩子 只需找到最后一个父亲节点开始调整 而wul

